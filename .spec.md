# 合同审批审查系统 Spec（草稿）

> 版本：v0.1 草稿（待评审）
> 依据：`项目实战.md` 2.4 合同审批审查系统、grill 会话确认的技术决策
> 状态：待用户确认后进入 `writing-plans`

## 1. 文档说明

- 目的：定义合同审批审查系统的范围、技术选型、模块边界、核心流程、数据库结构、接口契约、权限日志与验收标准。
- 范围：从审批系统拉取待办 → 下载合同附件 → 解析合同 → 规则审查 → 保存结果 → 评论回写 的完整闭环；管理端规则维护、日志查看、失败任务重试。
- 非目标：不代替人工审批；不接真实审批系统（demo 用 mock）；不做前端页面（本轮）；不做扫描件 OCR（二期）；不做部署与多实例（demo 跑通后另行决定）。
- 相关文档：`项目实战.md`、本文件；实施阶段另产出实施计划与 API 文档（FastAPI OpenAPI）。

## 2. 背景与目标

### 2.1 业务背景

面向企业合同审批场景，在人工审批前自动拉取待审批合同，解析合同内容并执行规则审查，生成风险审查意见后写回审批评论区，辅助法务审核人与审批人快速定位风险。

### 2.2 建设目标

- 能通过工具服务完成待办拉取、附件下载、合同解析、规则审查、结果保存、评论回写。
- 重复拉取按唯一业务标识去重，不产生重复任务。
- 异常（附件缺失、解析为空、接口失败等）进入 `blocked` 状态并支持人工重试。
- 每条风险命中保留原文证据与位置，支持追溯。
- 后端接口契约稳定，供后续前端对接。

### 2.3 非目标

- 不做自动审批决策，审查意见仅供参考。
- 不做合同模板库、条款库的长期运营（规则由规则表管理）。
- demo 阶段不实现真实审批系统鉴权与回调。

## 3. 技术选型

| 层 | 选型 | 说明 |
|---|---|---|
| 后端框架 | FastAPI + Uvicorn | 异步、Pydantic 校验、自动 OpenAPI |
| 编排 | LangGraph | 固定顺序图（graph 决定节点与流转，state 传递数据，节点内调用 LLM 能力）；不使用无限制自主循环 |
| LLM | OpenAI 兼容 API | `langchain-openai`/`openai` SDK，`model`/`base_url`/`api_key` 通过 `.env` 配置 |
| 数据库 | MySQL（Docker）+ SQLAlchemy 2.0 | 连接串可配置，模型按表结构定义 |
| 任务调度 | asyncio.Queue + FastAPI 后台任务 | 进程内队列，状态以 MySQL 表为准；预留可切换后端 |
| 文档解析 | pdfplumber + python-docx | 先提取文本层（按页/段落，保留页码与位置），再交给 LLM 抽取为结构化 JSON（字段 + 原文片段 + 位置）；扫描件 OCR 二期 |
| 规则引擎 | 混合模式 | `match_mode=regex/numeric` 走代码；`match_mode=llm` 走模型判断 |
| 审批对接 | Mock 适配层 | `MockClient` 实现审批系统适配接口，后续换 `RealClient` |
| 权限 | 简单角色 | 固定账号登录；`admin` 与 `reviewer` |
| 演示 | API + 演示脚本 + OpenAPI | 前端后续对接 |
| 测试 | pytest | 覆盖状态机、去重、规则引擎与关键工具函数 |

## 4. 模块划分

### 4.1 服务模块

| 模块 | 职责 |
|---|---|
| 拉取模块 | 调用审批适配层拉取待办；按 `approval_code` 去重入库 |
| 附件模块 | 附件下载、本地存储、附件元数据与下载状态管理 |
| 解析模块 | 文档文本提取（PDF/Word）、字段抽取（LLM）、解析状态与失败原因记录 |
| 规则模块 | 规则加载、代码规则匹配、LLM 规则判断、命中生成、总风险汇总 |
| 回写模块 | 评论内容生成、调用审批适配层写回、回写日志与状态 |
| 日志模块 | 全链路 `task_logs` 记录，按级别与类型分类 |
| 权限模块 | 账号注册与登录、token 校验、角色校验；注册账号默认 reviewer；admin 为种子账号 |
| 管理模块 | 规则 CRUD、任务重试、日志查询（`admin`） |

### 4.3 触发入口

- 对外提供"拉取待办"触发接口（`POST /api/tasks/trigger`），人工在前端点击按钮后启动拉取流程，随后自动进入后续步骤。
- 触发动作记录日志（`log_type=task`）。

### 4.2 审批系统适配层

定义统一接口，demo 由 `MockClient` 实现：

- `list_pending(limit)`：返回待审批单列表。
- `get_detail(instance_id)`：返回审批信息、表单数据、附件列表。
- `download(instance_id, attachment_id, file_name)`：返回本地附件路径与校验信息。
- `write_comment(instance_id, review_id)`：将审查意见写回评论区并返回结果。

数据来源：`mock/approvals.json`、`mock/attachments/`、`mock/comments/`。

## 5. 核心流程与状态机

### 5.1 主流程（正常路径）

```text
人工触发（前端点击"拉取代办"）→ 拉取待办 → 去重入库（pending）→ 下载主附件 → 解析（parsing）
→ 规则审查（reviewing）→ 保存结果 → 生成评论 → 回写（writing）
→ 回写成功（write_status=success，任务 done）
```

### 5.2 任务状态

- `pending`：已入库待处理。
- `parsing`：附件下载并执行文档解析。
- `reviewing`：执行规则审查与风险汇总。
- `blocked`：异常阻塞，等待人工重试。
- `done`：审查结果保存且回写流程结束（含回写失败但结果已保存）。

### 5.3 回写状态

- `not_written`：未开始回写。
- `writing`：回写中。
- `success`：回写成功。
- `failed`：回写失败（任务仍可为 `done`，回写可重试）。

### 5.4 异常与重试

- 附件缺失、下载失败、解析为空、图片无法识别、接口调用失败 → 任务 `blocked`，记录原因。
- `blocked` 任务由 `admin` 重试；按失败点回到 `parsing` 或 `reviewing`。
- 重复拉取：同一 `approval_code` 只更新已有记录与附件元数据，不新建任务。

## 6. 数据库设计

遵循项目数据库规范：主键统一 `id`；业务主表含审计与逻辑删除字段；不设外键与额外索引；删除采用逻辑删除。`2.4.9` 未列出的审计字段按规范补齐，字段以本文件为准。

### 6.1 approval_tasks

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | bigint | 是 | 主键，自增 |
| approval_code | varchar(64) | 是 | 审批单唯一业务标识（去重键） |
| approval_title | varchar(255) | 是 | 标题 |
| applicant_name | varchar(64) | 是 | 申请人 |
| task_status | varchar(16) | 是 | pending/parsing/reviewing/blocked/done |
| write_status | varchar(16) | 是 | not_written/writing/success/failed |
| create_user_id | bigint | 是 | 创建人用户 ID |
| create_time | datetime | 是 | 创建时间 |
| update_user_id | bigint | 否 | 最后修改人用户 ID |
| update_time | datetime | 否 | 最后修改时间 |
| is_deleted | int | 是 | 0 未删除，1 已删除 |

### 6.2 approval_attachments

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | bigint | 是 | 主键，自增 |
| task_id | bigint | 是 | 关联任务 |
| file_name | varchar(255) | 是 | 文件名 |
| file_type | varchar(32) | 是 | pdf/docx 等 |
| file_path | varchar(512) | 是 | 本地存储路径 |
| download_status | varchar(16) | 是 | pending/downloaded/failed |
| create_user_id | bigint | 是 | 创建人用户 ID |
| create_time | datetime | 是 | 创建时间 |
| update_user_id | bigint | 否 | 最后修改人用户 ID |
| update_time | datetime | 否 | 最后修改时间 |
| is_deleted | int | 是 | 逻辑删除 |

### 6.3 contract_parses

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | bigint | 是 | 主键，自增 |
| task_id | bigint | 是 | 关联任务 |
| basic_info_json | json | 否 | 合同基本信息（标题、编号、主体、金额、币种、时间等） |
| clause_info_json | json | 否 | 条款信息（付款、交付、验收、违约、保密、数据、知产、争议） |
| parse_status | varchar(16) | 是 | pending/extracting/success/failed |
| parse_error | text | 否 | 解析失败原因 |
| create_time | datetime | 是 | 创建时间 |
| update_time | datetime | 否 | 最后修改时间 |

> 每个解析字段保留 `原文片段`、`页码或位置`、`提取状态`；解析失败必须记录失败原因。

### 6.4 review_rules

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | bigint | 是 | 主键，自增 |
| rule_code | varchar(64) | 是 | 规则编码，唯一 |
| rule_name | varchar(128) | 是 | 规则名称 |
| risk_level | varchar(8) | 是 | low/medium/high |
| rule_status | varchar(8) | 是 | enabled/disabled |
| match_mode | varchar(16) | 是 | regex/numeric/llm |
| match_text | text | 否 | 匹配表达式/阈值/LLM 判断提示 |
| suggestion_text | text | 否 | 命中后的建议文案 |
| create_time | datetime | 是 | 创建时间 |
| update_time | datetime | 否 | 最后修改时间 |

### 6.5 rule_hits

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | bigint | 是 | 主键，自增 |
| task_id | bigint | 是 | 关联任务 |
| rule_id | bigint | 是 | 关联规则 |
| evidence_text | text | 是 | 命中证据（原文片段） |
| evidence_position | varchar(255) | 是 | 位置（页码/条款名） |
| hit_status | varchar(16) | 是 | pending/confirmed/ignored；自动审查只产生 pending |
| create_time | datetime | 是 | 创建时间 |
| update_time | datetime | 否 | 最后修改时间 |

### 6.6 review_results

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | bigint | 是 | 主键，自增 |
| task_id | bigint | 是 | 关联任务 |
| overall_risk_level | varchar(8) | 是 | low/medium/high |
| summary_text | text | 是 | 中文摘要 |
| focus_points_json | json | 是 | 审批关注点数组 |
| comment_text | text | 否 | 最终回写评论 |
| create_time | datetime | 是 | 创建时间 |
| update_time | datetime | 否 | 最后修改时间 |

### 6.7 comment_logs

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | bigint | 是 | 主键，自增 |
| task_id | bigint | 是 | 关联任务 |
| write_status | varchar(16) | 是 | writing/success/failed |
| write_response_text | text | 否 | 回写响应内容 |
| create_time | datetime | 是 | 创建时间 |

### 6.8 task_logs

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | bigint | 是 | 主键，自增 |
| task_id | bigint | 否 | 关联任务，可为空 |
| log_level | varchar(8) | 是 | info/warn/error |
| log_type | varchar(16) | 是 | task/parse/rule/writeback/auth |
| log_content | text | 是 | 日志内容 |
| create_time | datetime | 是 | 创建时间 |

### 6.9 users（账号表）

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| id | bigint | 是 | 主键，自增 |
| username | varchar(64) | 是 | 登录账号，唯一 |
| password_hash | varchar(255) | 是 | 密码哈希 |
| role | varchar(16) | 是 | admin/reviewer |
| create_time | datetime | 是 | 创建时间 |
| update_time | datetime | 否 | 最后修改时间 |
| is_deleted | int | 是 | 逻辑删除 |

> 种子账号：`admin / 123456`（密码哈希存储）；注册接口创建的账号默认 `reviewer`。

## 7. 后端设计

### 7.1 目录结构（草案）

```text
contract-review/
├── main.py                 # FastAPI 入口（根目录）
├── app/
│   ├── config.py           # pydantic-settings，读取 .env
│   ├── db.py               # SQLAlchemy engine/session
│   ├── cache.py            # 内存缓存（用户信息、登录态）
│   ├── models/             # ORM 模型（9 张表）
│   ├── schemas/            # Pydantic 请求/响应
│   ├── api/                # 对外 REST 路由（auth/tasks/rules/hits/logs）
│   ├── tools/              # 7 个工具函数（纯函数，被图节点调用）
│   ├── graph/              # LangGraph 状态图：节点、边、state 定义
│   ├── agents/             # 解析/规则/回写节点的 LLM 逻辑封装
│   ├── services/           # 业务服务：拉取、附件、解析、规则、回写、日志、认证
│   ├── adapters/           # 审批系统适配层（MockClient/RealClient）
│   ├── core/               # 状态机、常量、权限依赖
│   └── utils/              # 文本、文件、校验工具
├── mock/
│   ├── approvals.json
│   ├── attachments/            # 样例合同 PDF/docx
│   └── comments/
├── uploads/                    # 运行时下载的附件
├── tests/
├── .env.example
├── requirements.txt
└── README.md
```

### 7.2 各层职责

- `tools`：7 个工具为纯函数式实现，输入输出可校验，被 LangGraph 节点调用，不依赖具体业务服务实现。
- `graph`：固定顺序状态图，节点按主流程排列，异常边指向 `blocked`；`state` 是节点间唯一数据通道。
- `agents`：节点内调用 LLM 的逻辑（字段抽取、语义规则、评论生成），统一走 LLM 适配层；与 `graph` 解耦，节点只依赖其接口。
- `services`：业务编排与持久化；`services/review_hits.py` 预留 `confirm/ignore` 接口（demo 不启用）。
- `adapters`：审批系统接口定义与 Mock 实现；后续新增 RealClient 无需改上层。
- `cache.py`：进程内 TTL 缓存，缓存用户信息与登录态（demo 无 Redis）。
- `core/state_machine.py`：任务状态与回写状态常量、允许的流转。
- 解耦约定：`api → services → tools/graph/adapters`，同层之间不互相依赖具体实现；模块间通过 schemas/模型交互。

## 8. 接口设计

### 8.1 内部工具函数

| 函数 | 输入 | 输出 |
|---|---|---|
| list_pending_contract_approvals | limit | 待办列表（审批编号、标题、申请人、申请时间、附件数量） |
| get_contract_approval | instance_id | 审批信息、表单数据、附件信息、处理状态 |
| download_contract_attachment | instance_id, attachment_id, file_name | 本地路径、校验信息 |
| parse_contract_document | document_id | 结构化字段、原文片段、定位信息 |
| run_contract_rules | case_id | 命中结果、风险等级、建议 |
| save_review_result | case_id, overall_risk_level, summary_text, focus_points_json, comment_text | 保存结果 |
| write_approval_comment | instance_id, review_id | 回写结果 |

### 8.2 对外 REST 接口（前端对接面）

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| POST | /api/auth/register | 公开 | 注册账号（username/password），默认 reviewer |
| POST | /api/auth/login | 公开 | 固定账号登录，返回 token |
| GET | /api/tasks | admin/reviewer | 待办/任务列表（按状态筛选、分页） |
| GET | /api/tasks/{task_id} | admin/reviewer | 任务详情（含附件、解析、命中、结果） |
| POST | /api/tasks/{task_id}/retry | admin | 重试 blocked 任务 |
| GET | /api/tasks/{task_id}/attachments | admin/reviewer | 附件列表 |
| GET | /api/rules | admin/reviewer | 规则列表 |
| POST | /api/rules | admin | 新增规则 |
| PUT | /api/rules/{rule_id} | admin | 编辑规则 |
| DELETE | /api/rules/{rule_id} | admin | 删除规则（逻辑删除） |
| GET | /api/hits | admin/reviewer | 命中记录（可按任务/规则/风险等级筛选） |
| POST | /api/hits/{hit_id}/confirm | admin（预留） | 确认命中（demo 不启用） |
| POST | /api/hits/{hit_id}/ignore | admin（预留） | 忽略命中（demo 不启用） |
| GET | /api/logs | admin | 运行日志（按级别/类型/任务筛选） |
| POST | /api/tasks/trigger | admin | 手动触发一次拉取（演示用） |

接口响应统一结构：`{ code, message, data }`；错误场景返回明确的错误码与提示。

### 8.3 环境变量（.env）

所有客户端/服务配置统一放 `.env`，项目提供 `.env.example`：

```text
# LLM（OpenAI 兼容）
LLM_API_KEY=xxx
LLM_BASE_URL=https://api.example.com/v1
LLM_MODEL=gpt-4o-mini
# 预留按方向各配一个模型（当前统一用 LLM_MODEL）
# LLM_PARSE_MODEL=
# LLM_RULE_MODEL=
# LLM_WRITEBACK_MODEL=

# MySQL（Docker）
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=xxx
DB_NAME=contract_review

# 服务
APP_HOST=0.0.0.0
APP_PORT=8000
SECRET_KEY=xxx
TOKEN_EXPIRE_MINUTES=1440

# 权限
ADMIN_USERNAME=admin
ADMIN_PASSWORD=123456

# 审批适配与存储
APPROVAL_ADAPTER=mock
MOCK_DATA_DIR=mock
UPLOAD_DIR=uploads
LOG_DIR=logs
```

## 9. 规则设计

### 9.1 种子规则（覆盖 2.4.6）

| 规则 | match_mode | 说明 |
|---|---|---|
| 预付款比例过高 | numeric | 阈值判断，如 > 50% 高危 |
| 付款周期过长 | numeric | 阈值判断 |
| 自动续约 | llm | 判断是否存在不合理自动续约条款 |
| 违约责任缺失/不对等 | llm | 判断违约条款是否存在且对等 |
| 管辖地异常 | regex/llm | 关键词（境外、仲裁地）或语义判断 |
| 主体信息缺失 | regex | 签约主体字段缺失 |
| 金额缺失 | regex | 金额字段缺失 |
| 保密条款缺失 | llm | 判断保密条款是否存在 |
| 数据处理缺失 | llm | 判断数据条款是否存在 |
| 知识产权缺失 | llm | 判断知识产权条款是否存在 |
| 验收标准缺失 | llm | 判断验收条款是否实质存在 |

### 9.2 规则引擎执行流程

规则引擎不是"代码规则与 LLM 规则分开跑"，而是所有规则进入同一个流水线，按阶段依次执行：

```text
阶段 1 代码匹配（确定性优先）
  - 加载 enabled 规则，按成本/优先级排序
  - regex/numeric 规则对解析结果执行确定性匹配
  - 命中 → 生成 rule_hits（pending，带证据与位置）；未命中 → 仅记录

阶段 2 LLM 语义审查
  - 汇总解析字段与阶段 1 结果，构造统一审查上下文
  - 对 llm 类规则逐条判断（命中/未命中 + 证据 + 理由）
  - 可选：对阶段 1 命中的证据做语义复核，过滤明显误报
  - LLM 输出同样写入 rule_hits（pending）

阶段 3 风险汇总
  - 按命中规则的 risk_level 聚合总风险等级
  - 生成命中规则列表、中文摘要、审批关注点

阶段 4 人工复核（框架预留，demo 不启用）
  - hits 初始 pending；confirm/ignore 接口已定义，后续启用
```

### 9.3 风险汇总规则

- 命中任一 high 规则 → 总风险 `high`。
- 否则命中多个 medium 规则 → `medium`。
- 其余 → `low`。
- 审查输出：总风险等级、命中规则列表（名称、等级、证据、位置、建议）、中文摘要、审批关注点、回写内容。

## 10. 权限与日志

- 注册：`POST /api/auth/register` 只填账号与密码，创建 `reviewer` 账号；账号写入 `users` 表并缓存用户信息。
- 登录：`POST /api/auth/login` 校验密码，签发 token；用户信息缓存用于后续鉴权，缓存未命中时回源数据库。
- 种子账号：`admin / 123456`（密码哈希存储，账号与密码可经 `.env` 覆盖）。
- 接口按上表权限控制；未登录返回 401，无权限返回 403。
- 注册、登录、规则变更、任务重试、命中确认等关键操作写入 `task_logs`（`log_type=auth/admin`）。
- 任务全链路按 `task/parse/rule/writeback` 类型记录 `info/warn/error` 日志，内容包含 `task_id` 等业务标识。

## 11. 验收标准

- 能通过工具服务拉取待处理审批单，并按 `approval_code` 去重。
- 能下载合同附件并保存附件记录。
- 能解析 PDF/Word 合同，提取基本信息与条款，保留原文片段与位置；解析失败有原因记录。
- 能执行混合规则审查，返回命中证据、风险等级与建议。
- 能保存审查结果并生成评论，通过 mock 审批适配层回写评论区。
- 异常任务进入 `blocked` 并支持 `admin` 重试。
- 无 LLM 配置时能启动服务并提示（部署文档说明）；配置 key 后完成真实 LLM 闭环。
- 完整闭环演示：拉取 → 下载 → 解析 → 规则 → 保存 → 回写，提供演示脚本与 OpenAPI 文档。
- pytest 覆盖：状态机流转、去重、代码规则匹配、mock 适配层、关键工具函数。
- 交付 README：MySQL 启动、`.env` 填写、依赖安装、服务启动、mock 演示步骤。

## 12. 待确认项

- 样例合同的具体条款设计（高/中/低风险各 1 份，按实际审查效果调整）。
- 前端对接形式（Vue/其他）与页面清单，待后端跑通后确定。
- 真实审批系统对接方式与鉴权，待有真实环境后确定。
- 扫描件 OCR 的引入时机（二期）。
