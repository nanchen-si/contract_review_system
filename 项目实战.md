# 项目实战
## 项目实战要求
- 不限制完成方式
- 提交时文件夹中放代码文件（不带依赖）和测试截图，文件夹以“组名_姓名_项目_行业”命名，如“3组_张三_客服_教育”

## 1. 基础项目
### 1.1 知识库项目实战
- 项目目标：实现听书、教育、旅游、育儿、金融领域的知识库

| 领域 | 主要内容                                                             |
| ---- | -------------------------------------------------------------------- |
| 听书 | 听书知识库需求和图书简介资料                                         |
| 教育 | 教育知识库需求、课程资料、课程文档和项目文档                         |
| 旅游 | 旅游知识库需求，以及交通、景点、线路、美食和酒店资料                 |
| 育儿 | 育儿知识库需求，以及专家建议、亲子案例、沟通话术、知识科普和育儿建议 |
| 金融 | 金融知识库需求，以及年报、基金、政策、用户 FAQ 和风险揭示资料        |

### 1.2 问数项目实战
- 项目目标：实现听书、教育、旅游、金融领域的问数系统

| 领域 | 数据生成       |
| ---- | -------------- |
| 听书 | `audio-data`   |
| 教育 | `edu-data`     |
| 旅游 | `travel-data`  |
| 金融 | `finance-data` |

- 交付要求：生成业务数据，不需要启动后端服务

### 1.3 客服项目实战
- 项目目标：实现听书、教育、旅游、金融领域的智能客服系统

| 领域 | 业务接口提供   |
| ---- | -------------- |
| 听书 | `audio-data`   |
| 教育 | `edu-data`     |
| 旅游 | `travel-data`  |
| 金融 | `finance-data` |

- 交付要求：启动后端服务

## 2. 其他项目
### 2.1 经营归因分析系统
- 项目目标：实现一个面向经营分析场景的多轮归因分析系统，支持围绕一个业务问题持续追问、补充证据、生成阶段性结论和最终分析报告
- 交付形式：提供可运行前端、后端、认证服务、数据库初始化脚本、示例数据、接口说明和至少两组完整分析示例

#### 2.1.1 开发范围
- 基础能力：认证中心登录、会话管理、附件管理、配置热更新、运行日志
- 分析能力：实时分析、数据库查询、文件读写、文本检索、命令执行、结果文件生成
- 交付能力：聊天工作台、结果保存、结果导出、至少两个业务分析场景的示例数据和完整演示链路

#### 2.1.2 用户角色
- `分析用户`：创建会话、输入分析问题、上传资料、查看历史消息、查看分析结果、下载结果文件
- `系统管理员`：维护认证配置、维护系统配置、查看运行日志、管理数据源连接、启停功能开关

#### 2.1.3 页面需求
- `授权登录入口页`：展示登录按钮，点击后跳转认证中心完成授权登录
- `登录回调页`：处理授权回调参数、换取访问令牌、保存登录态并自动跳转到聊天工作台
- `聊天工作台页`：左侧展示会话列表，中间展示当前会话对话区，右侧展示本轮分析结果，底部提供问题输入框和发送按钮
- `附件侧栏`：展示当前会话下的附件列表，字段至少包含文件名、文件类型、文件大小、上传时间、解析状态，支持上传、删除、下载
- `实时任务区`：展示当前分析轮状态、当前步骤、错误信息、工具执行过程和结果文件生成状态
- `结果展示区`：展示问题定义、关键指标、证据列表、归因结论、待补充数据、下一步建议，支持复制和导出

#### 2.1.4 会话与任务规则
- 会话状态至少包含 `active`、`archived`、`deleted`
- 分析任务状态至少包含 `queued`、`running`、`success`、`failed`、`cancelled`
- 一条用户消息对应一次分析任务
- 删除会话时，同时删除该会话下的消息、附件记录、任务记录、结果记录和上下文摘要记录
- 同一时间一个会话只允许存在一个运行中的分析任务
- 分析任务通过长连接实时返回中间消息和最终结果

#### 2.1.5 前端模块要求
- `认证回调模块`：处理授权跳转、回调参数接收、访问令牌保存、登录态校验
- `会话模块`：处理会话列表加载、新建、删除、重命名、切换
- `聊天模块`：处理消息发送、消息流式渲染、取消分析、历史消息回放
- `附件模块`：处理文件上传、进度展示、删除、下载
- `结果模块`：处理结构化结果展示、结果复制、结果导出
- `配置模块`：处理系统配置查看和配置重载
- `日志模块`：处理运行状态展示和错误信息查看

#### 2.1.6 后端模块要求
- `认证接入模块`：负责对接认证中心、令牌校验、当前用户识别
- `会话模块`：负责会话的新增、查询、更新、删除
- `消息模块`：负责消息入库、历史消息查询、消息顺序控制
- `附件模块`：负责文件保存、删除、下载、路径校验
- `长连接模块`：负责会话级实时连接、连接鉴权、消息推送和连接关闭
- `任务模块`：负责分析任务创建、状态流转、取消执行、日志记录、临时令牌签发
- `分析模块`：负责拼装上下文、执行查询和文件工具、生成结构化结论
- `结果模块`：负责分析结果落库、结果导出、结果文件管理、上下文摘要压缩
- `配置模块`：负责系统配置读取和热更新

#### 2.1.7 数据表要求
- `users`：`id`、`external_user_id`、`username`、`display_name`、`role`、`status`、`created_at`、`updated_at`
- `conversations`：`id`、`user_id`、`title`、`status`、`last_message_at`、`created_at`、`updated_at`
- `messages`：`id`、`conversation_id`、`role`、`message_type`、`content`、`tool_name`、`tool_status`、`seq_no`、`created_at`
- `attachments`：`id`、`conversation_id`、`message_id`、`file_name`、`file_path`、`file_type`、`file_size`、`parse_status`、`created_at`
- `analysis_tasks`：`id`、`conversation_id`、`user_id`、`input_text`、`task_status`、`current_step`、`started_at`、`finished_at`、`error_message`
- `analysis_results`：`id`、`task_id`、`conversation_id`、`problem_definition`、`key_metrics_json`、`evidence_list_json`、`conclusion_text`、`missing_data_text`、`next_action_text`、`result_markdown`、`result_file_path`、`created_at`
- `context_summaries`：`id`、`conversation_id`、`start_seq_no`、`end_seq_no`、`summary_text`、`created_at`
- `websocket_tokens`：`id`、`user_id`、`conversation_id`、`token`、`expires_at`、`consumed_at`、`created_at`
- `system_configs`：`id`、`config_key`、`config_value`、`config_group`、`updated_at`
- `task_logs`：`id`、`task_id`、`log_level`、`log_type`、`log_content`、`created_at`

#### 2.1.8 文件存储要求
- 附件按 `uploads/{user_id}/{conversation_id}/` 目录存储
- 导出结果按 `exports/{user_id}/{conversation_id}/` 目录存储
- 临时中间文件按 `workspace/{user_id}/{conversation_id}/` 目录存储
- 删除会话时，同时删除该会话对应的附件目录、导出目录和临时目录

#### 2.1.9 接口要求
- `GET /auth/login`：跳转到认证中心发起授权登录
- `GET /auth/callback`：处理授权回调并建立登录态
- `POST /api/chat/create`：请求字段 `title`，响应字段 `conversation_id`、`title`、`status`
- `POST /api/chat/delete`：请求字段 `conversation_ids`
- `POST /api/chat/update`：请求字段 `conversation_id`、`title`
- `GET /api/chat/ls`：响应字段 `conversation_id`、`title`、`status`、`last_message_at`
- `GET /api/chat/ls/{conversation_id}`：响应字段 `message_id`、`role`、`content`、`attachments`、`created_at`
- `POST /api/attachment/upload`：请求字段 `conversation_id` 和文件对象，响应字段 `attachment_id`、`file_name`、`file_path`
- `POST /api/attachment/delete`：请求字段 `attachment_id`
- `GET /api/attachment/get`：请求字段 `attachment_id`
- `POST /api/chat/ws-token`：响应字段 `websocket_token`、`expires_in`
- `WS /api/chat/ws/chat`：连接参数 `websocket_token`、`conversation_id`
- `POST /api/admin/reload`：响应字段 `status`、`message`
- `GET /api/tasks/{task_id}`：响应字段 `task_status`、`current_step`、`started_at`、`finished_at`、`error_message`
- `GET /api/results/{task_id}`：响应字段 `problem_definition`、`key_metrics`、`evidence_list`、`conclusion_text`、`missing_data_text`、`next_action_text`

#### 2.1.10 实时消息类型
- `message_start`：表示本轮分析开始，字段至少包含 `task_id`、`conversation_id`
- `message_delta`：表示模型增量文本，字段至少包含 `task_id`、`delta_text`
- `tool_start`：表示某个工具开始执行，字段至少包含 `task_id`、`tool_name`
- `tool_finish`：表示某个工具执行完成，字段至少包含 `task_id`、`tool_name`、`tool_result_summary`
- `task_status`：表示任务状态变化，字段至少包含 `task_id`、`task_status`、`current_step`
- `result_ready`：表示结构化结果已生成，字段至少包含 `task_id`、`result_id`
- `error`：表示本轮分析失败，字段至少包含 `task_id`、`error_message`
- `done`：表示本轮分析结束，字段至少包含 `task_id`、`finished_at`

#### 2.1.11 分析输出格式
- `问题定义`：当前分析要回答的业务问题
- `关键指标`：以数组保存，每项至少包含 `metric_name`、`metric_value`、`metric_unit`、`metric_period`
- `证据列表`：以数组保存，每项至少包含 `source_type`、`source_name`、`evidence_text`、`related_metric`、`confidence`
- `归因结论`：用自然语言输出主要原因和影响范围
- `待补充数据`：列出当前分析仍缺失的数据项
- `下一步建议`：列出后续建议动作，至少 2 条

#### 2.1.12 示例业务场景要求
- `商品目录优化`：至少提供商品表、类目表、搜索曝光表、点击表、转化表
- `客户行为分析`：至少提供用户表、访问事件表、加购事件表、下单事件表
- `库存异常分析`：至少提供库存表、入库表、出库表、销量表
- `评论反馈分析`：至少提供评论表、评分表、商品表、退款表
- `市场表现分析`：至少提供渠道表、投放表、订单表、销售汇总表
- `退款模式分析`：至少提供退款申请表、退款原因表、订单表、用户表
- 至少任选两个场景完成全链路演示

#### 2.1.13 验收标准
- 能通过授权登录进入聊天工作台
- 能创建会话、上传附件、发送消息并查看历史记录
- 能通过实时连接看到分析过程中的状态变化、工具执行和最终结果
- 能查询历史消息、切换旧会话并继续追问
- 能在结果区看到六部分结构化输出
- 能导出分析结果文件并重新下载
- 能通过配置重载接口更新配置并即时生效
- 能完成至少两个业务场景的完整演示

### 2.2 舆情分析平台
- 项目目标：实现一个面向指定主题的舆情分析平台，从公开信息、多媒体内容、内部沉淀数据三个方向并行收集信息，汇总生成结构化舆情报告
- 交付形式：提供可运行 Web 控制台、分析服务、配置文件、示例数据、接口说明和至少一个完整主题的分析案例

#### 2.2.1 开发范围
- 控制台能力：系统启停、单功能应用启停、状态查看、输出查看、配置管理
- 分析能力：主题检索、多媒体检索、论坛采集、洞察分析、报告生成、图谱查询
- 展示能力：应用输出查看、论坛日志查看、图谱查看器、系统状态面板

#### 2.2.2 用户角色
- `操作用户`：启动和停止单功能应用、发起主题检索、查看输出结果
- `报告查看人`：查看已生成报告、图谱结果、论坛日志和来源证据
- `系统管理员`：维护系统配置、查看系统状态、启停整体服务、查看运行日志

#### 2.2.3 页面需求
- `控制台首页`：展示各单功能应用状态、启动按钮、停止按钮、最近输出摘要和系统状态
- `主题检索页`：输入主题词、关键词或检索条件，发起主题分析并查看文本输出
- `论坛监控页`：展示论坛采集启动状态、最新日志、历史日志查询入口
- `图谱查看页`：展示最新图谱和指定报告图谱，支持节点查询和关系查看
- `配置管理页`：展示系统配置、采集配置和运行参数，支持读取与保存
- `系统状态页`：展示整体服务状态、各应用状态、最近错误信息和资源运行概况

#### 2.2.4 任务状态与执行流程
- 单功能应用状态至少包含 `stopped`、`starting`、`running`、`stopping`、`failed`
- 论坛采集状态至少包含 `idle`、`running`、`stopped`、`failed`
- 系统状态至少包含 `offline`、`starting`、`online`、`shutting_down`
- 系统启动后，允许独立启动主题检索、多媒体检索、洞察分析等单功能应用
- 每个单功能应用都要单独保存运行状态、标准输出和错误输出
- 停止应用后，不再继续接收新任务，但已生成的输出结果和日志保留
- 图谱数据和报告结果要支持按报告编号回查

#### 2.2.5 前端模块要求
- `控制台模块`：处理应用状态查询、应用启停、系统状态展示
- `输出模块`：处理单功能应用输出查看、测试日志查看、滚动展示
- `论坛模块`：处理论坛任务启停、论坛日志查看、历史日志查询
- `图谱模块`：处理图谱查看、报告切换、图查询结果展示
- `检索模块`：处理主题检索请求、结果展示和来源查看
- `配置模块`：处理配置读取、更新、回显

#### 2.2.6 后端模块要求
- `控制层模块`：负责 Web 路由、页面渲染、状态接口和系统启停入口
- `进程管理模块`：负责单功能应用启动、停止、状态采集、输出采集
- `检索分析模块`：负责主题检索、内容分析、洞察生成和报告汇总
- `论坛模块`：负责论坛采集任务控制、日志输出和历史记录保存
- `图谱模块`：负责图谱结果加载、指定报告查询和关系检索
- `配置模块`：负责配置读取、保存和运行参数热更新

#### 2.2.7 数据存储要求
- `config.json`：保存系统配置、运行参数和默认应用开关
- `runtime/apps/{app_name}.log`：保存各单功能应用标准输出和错误输出
- `runtime/forum/latest.log`：保存论坛采集最新运行日志
- `runtime/forum/history/{date}.json`：保存论坛日志历史记录
- `reports/{report_id}/report.md`：保存结构化分析报告
- `graphs/{report_id}/graph.json`：保存图谱节点和边关系数据
- `outputs/{app_name}/latest.txt`：保存单功能应用最近一次文本输出结果

#### 2.2.8 采集与分析规则
- 每次主题检索都必须至少保存主题词、执行时间、来源摘要和分析输出
- 支持来源类型至少包含 `news`、`image`、`video`、`forum_post`、`internal_data`
- 图谱结果至少保存节点标识、节点类型、关系类型和来源引用
- 论坛日志至少保留时间、事件类型、消息内容和任务状态
- 最终报告中的结论必须可以追溯到来源内容、论坛记录或图谱节点

#### 2.2.9 接口要求
- `GET /api/status`：查询控制台状态和各应用运行状态
- `GET /api/start/{app_name}`：启动指定单功能应用
- `GET /api/stop/{app_name}`：停止指定单功能应用
- `GET /api/output/{app_name}`：查询指定应用最近输出
- `GET /api/test_log/{app_name}`：查询指定应用测试日志
- `POST /api/search`：请求字段 `query`、`source_types`，返回主题检索和分析结果
- `GET /api/forum/start`：启动论坛采集
- `GET /api/forum/stop`：停止论坛采集
- `GET /api/forum/log`：查询论坛最新日志
- `POST /api/forum/log/history`：按日期查询论坛历史日志
- `GET /api/config`：查询系统配置
- `POST /api/config`：更新系统配置
- `GET /api/system/status`：查询整体服务状态
- `POST /api/system/start`：启动整体服务
- `POST /api/system/shutdown`：关闭整体服务
- `GET /api/graph/latest`：查询最新图谱结果
- `GET /api/graph/{report_id}`：查询指定报告图谱
- `POST /api/graph/query`：按节点或关系条件查询图谱
- `GET /graph-viewer`：进入图谱查看页
- `GET /graph-viewer/{report_id}`：进入指定报告图谱查看页

#### 2.2.10 实时消息类型
- `app_status`：字段至少包含 `app_name`、`status`
- `app_output`：字段至少包含 `app_name`、`output_text`
- `forum_log`：字段至少包含 `message_text`、`task_status`
- `system_status`：字段至少包含 `system_status`、`running_apps`
- `graph_ready`：字段至少包含 `report_id`、`graph_path`
- `error`：字段至少包含 `module_name`、`error_message`

#### 2.2.11 报告输出格式
- `事件概述`：说明主题背景和分析范围
- `时间线`：按时间顺序列出关键事件
- `传播渠道`：按来源类型统计传播分布
- `主要观点`：列出不同来源和不同角色总结出的核心观点
- `情绪倾向`：给出正向、中性、负向的整体判断
- `风险判断`：列出高风险点和关注事项
- `重点证据`：列出来源标题、链接、摘要和引用位置
- `结论摘要`：给出最终结论和建议

#### 2.2.12 验收标准
- 能通过控制台启动和停止单功能应用
- 能查看应用状态、应用输出和最近错误信息
- 能启动论坛采集并查看最新日志和历史日志
- 能发起主题检索并看到结构化分析结果
- 能查看最新图谱和指定报告图谱
- 能更新配置并即时生效
- 能完成至少一个完整主题的检索、分析、图谱查看和结果展示演示

### 2.3 电商风险控制系统
- 项目目标：实现一个面向电商业务的实时风险控制平台，覆盖风险识别、规则判断、人工复核、处置留痕和结果查询的完整闭环
- 交付形式：提供可运行前端、后端、数据库初始化脚本、示例数据、接口说明和完整演示链路

#### 2.3.1 开发范围
- 检测能力：事件接入、特征计算、规则匹配、风险评分
- 处置能力：案件生成、案件审核、黑名单管理、用户画像
- 管理能力：规则增删改查、命中记录查询、审核日志记录、统计报表、运营看板

#### 2.3.2 用户角色
- `风控审核员`：查看风险结果、审核案件、填写处理结论
- `风控管理员`：维护规则、维护黑名单、查看统计看板
- `数据查看人`：查看用户画像、查看历史风险记录、查看命中统计

#### 2.3.3 页面需求
- `风险检查页`：输入事件类型、用户编号、订单编号等参数，返回评分、风险等级、命中规则、特征快照
- `规则管理页`：展示规则名称、规则状态、优先级、命中次数、更新时间，支持新增、编辑、启停用、删除
- `案件管理页`：展示案件编号、案件状态、风险等级、关联用户、关联订单、审核人、创建时间
- `案件详情页`：展示风险结果、命中规则、特征快照、审核记录、处理结论
- `黑名单页`：展示黑名单类型、命中值、备注、创建时间，支持新增、删除、批量导入
- `用户画像页`：展示历史订单、退款次数、投诉次数、地址数量、最近风险事件、关联案件
- `运营看板页`：展示风险事件总数、高风险占比、案件处理效率、规则命中排行、黑名单命中次数

#### 2.3.4 业务事件与状态规则
- 业务事件至少包含 `order_create`、`order_pay`、`after_sale_apply`、`logistics_complaint`
- 风险等级至少包含 `low`、`medium`、`high`
- 处理建议至少包含 `pass`、`manual_review`、`reject`
- 案件状态至少包含 `pending`、`reviewing`、`approved`、`rejected`、`resolved`
- 当风险等级为 `high` 或处理建议为 `manual_review`、`reject` 时，自动创建案件
- 每次风险检查都必须保存原始入参、特征快照、命中规则和最终结果

#### 2.3.5 风险规则要求
- 至少实现 25 个风险特征，覆盖用户维度、订单维度、地址维度
- 至少实现 24 条规则，规则需支持启停用、优先级、命中分值、命中描述
- 单条规则至少包含 `rule_name`、`rule_code`、`rule_status`、`priority`、`score`、`condition_json`、`description`
- 规则条件至少支持大于、小于、等于、包含、与、或这些基础判断
- 特征快照需按键值对保存，便于后续回看和问题排查

#### 2.3.6 前端模块要求
- `检查模块`：处理风险检查请求、结果展示、特征快照渲染
- `规则模块`：处理规则列表、新增编辑表单、启停用操作
- `案件模块`：处理案件列表、案件详情、审核提交
- `黑名单模块`：处理黑名单列表、新增、删除、导入
- `画像模块`：处理用户画像查询和历史记录展示
- `看板模块`：处理统计卡片、趋势图、排行图

#### 2.3.7 后端模块要求
- `事件模块`：负责接收风险检查请求、校验入参、写入事件记录
- `特征模块`：负责根据业务数据生成风险特征快照
- `规则模块`：负责规则管理、规则匹配、命中结果生成
- `决策模块`：负责根据命中规则和评分输出风险等级与处理建议
- `案件模块`：负责案件创建、审核、状态更新、日志记录
- `画像模块`：负责用户画像聚合查询
- `统计模块`：负责看板指标计算和趋势统计

#### 2.3.8 数据表要求
- `risk_events`：`id`、`event_type`、`source_id`、`user_id`、`order_id`、`event_payload_json`、`created_at`
- `risk_assessments`：`id`、`event_id`、`risk_score`、`risk_level`、`decision`、`assessment_status`、`created_at`
- `feature_snapshots`：`id`、`assessment_id`、`feature_json`、`created_at`
- `risk_rules`：`id`、`rule_code`、`rule_name`、`rule_status`、`priority`、`score`、`condition_json`、`description`、`updated_at`
- `rule_hits`：`id`、`assessment_id`、`rule_id`、`hit_score`、`hit_message`、`created_at`
- `risk_cases`：`id`、`assessment_id`、`user_id`、`order_id`、`case_status`、`reviewer_id`、`review_result`、`created_at`、`updated_at`
- `blacklists`：`id`、`blacklist_type`、`blacklist_value`、`remark`、`status`、`created_at`
- `review_logs`：`id`、`case_id`、`operator_id`、`action_type`、`action_remark`、`created_at`

#### 2.3.9 接口要求
- `POST /api/risk/check`：请求字段 `event_type`、`source_id`、`user_id`、`order_id`
- `GET /api/risk/assessments/{assessment_id}`：响应字段 `risk_score`、`risk_level`、`decision`、`feature_snapshot`、`rule_hits`
- `GET /api/risk/rules`：查询规则列表
- `POST /api/risk/rules`：新增规则
- `POST /api/risk/rules/update`：更新规则
- `POST /api/risk/rules/status`：启停用规则
- `POST /api/risk/rules/delete`：删除规则
- `GET /api/risk/cases`：查询案件列表
- `GET /api/risk/cases/{case_id}`：查询案件详情
- `POST /api/risk/cases/review`：请求字段 `case_id`、`review_result`、`review_remark`
- `GET /api/risk/blacklists`：查询黑名单列表
- `POST /api/risk/blacklists`：新增黑名单
- `POST /api/risk/blacklists/delete`：删除黑名单
- `GET /api/risk/users/{user_id}/profile`：查询用户画像
- `GET /api/risk/dashboard`：查询看板统计

#### 2.3.10 输出结果格式
- `风险评分`：0 到 100 的数值
- `风险等级`：低、中、高
- `处理建议`：放行、人工审核、拒绝
- `命中规则`：数组，每项至少包含规则名称、规则分值、命中原因
- `特征快照`：键值对结构
- `审核结果`：审核人、审核结论、审核备注、审核时间

#### 2.3.11 验收标准
- 能发起四类业务事件的风险检查
- 能生成评分、等级、处理建议、命中规则和特征快照
- 高风险结果能自动创建案件
- 能在案件页完成审核并查看审核日志
- 能新增、编辑、启停用规则并看到命中统计变化
- 能维护黑名单并在风险检查中生效
- 能查看用户画像和运营看板
- 能完成一条完整演示链路
- 事件进入后，能够查看规则命中、案件生成、人工审核和结果回查

### 2.4 合同审批审查系统
- 项目目标：实现一个面向企业合同审批场景的自动审查系统，在不直接代替人工审批的前提下生成风险审查意见并写回审批评论区
- 交付形式：提供可运行工具服务、数据库初始化脚本、示例合同文件、工具调用说明和完整闭环演示

#### 2.4.1 开发范围
- 接入能力：审批单拉取、合同附件下载、重复任务去重
- 审查能力：合同解析、扫描件文字识别、规则审查
- 输出能力：结果保存、评论回写、异常阻塞处理、运行日志

#### 2.4.2 用户角色
- `审批系统`：触发待办拉取、提供审批详情、接收评论回写结果
- `法务审核人`：查看合同详情、查看证据定位、判断风险关注点
- `系统管理员`：维护审批接入配置、维护规则、查看运行日志、重试失败任务

#### 2.4.3 服务入口需求
- `待办拉取工具`：返回待处理审批单列表，字段至少包含审批编号、标题、申请人、申请时间、附件数量
- `审批详情工具`：返回单个审批单详情，字段至少包含审批信息、表单数据、附件信息和当前处理状态
- `附件下载工具`：按审批编号和附件编号下载合同附件，并返回本地文件路径和校验信息
- `文档解析工具`：对已下载合同执行解析，输出结构化字段、原文片段和定位信息
- `规则审查工具`：对解析结果执行规则匹配，输出风险等级、命中证据和处理建议
- `结果保存工具`：保存审查结果、命中规则和摘要信息
- `评论回写工具`：将最终审查意见写回审批评论区，并返回回写结果

#### 2.4.4 任务状态与流转规则
- 任务状态至少包含 `pending`、`parsing`、`reviewing`、`blocked`、`done`
- 回写状态至少包含 `not_written`、`writing`、`success`、`failed`
- 每个审批单按唯一业务标识去重，重复拉取时只更新已有记录，不重复创建新任务
- 附件缺失、图片无法识别、文档内容为空、接口调用失败都要进入 `blocked` 状态
- `blocked` 状态允许人工重试，重试后重新进入 `parsing` 或 `reviewing`

#### 2.4.5 合同解析字段要求
- 合同基本信息至少包含合同标题、合同编号、签约主体、对方名称、金额、币种、生效时间、到期时间
- 条款信息至少包含付款条款、交付条款、验收条款、违约条款、保密条款、数据条款、知识产权条款、争议解决条款
- 每个解析字段至少保留原文片段、页码或位置标识、提取状态
- 解析失败时，要记录失败原因，不允许只返回空结果

#### 2.4.6 审查规则要求
- 单条规则至少包含 `rule_code`、`rule_name`、`risk_level`、`rule_status`、`match_mode`、`match_text`、`suggestion_text`
- 规则至少覆盖预付款比例、付款周期、自动续约、违约责任、管辖地、主体信息缺失、金额缺失、保密缺失、数据处理、知识产权、验收标准缺失
- 命中结果至少包含规则名称、风险等级、命中证据、证据位置、建议说明
- 审查输出必须包含总风险等级和审批关注点列表

#### 2.4.7 调用端能力要求
- `待办调用模块`：调用待办拉取工具并展示待审列表
- `详情查看模块`：调用审批详情和附件下载工具，展示审批信息和附件信息
- `解析结果模块`：调用文档解析工具并展示字段结果、原文片段和定位信息
- `规则命中模块`：调用规则审查工具并展示风险等级、命中证据和建议
- `结果处理模块`：调用结果保存和评论回写工具，展示回写状态和最终输出

#### 2.4.8 服务模块要求
- `拉取模块`：负责拉取待处理审批单和去重保存
- `附件模块`：负责附件下载、附件存储、附件元数据保存
- `解析模块`：负责文档解析、图片识别、字段提取
- `规则模块`：负责规则匹配、命中生成、风险汇总
- `回写模块`：负责生成评论内容、写回审批评论区、保存回写日志
- `日志模块`：负责记录全链路日志和错误信息

#### 2.4.9 数据表要求
- `approval_tasks`：`id`、`approval_code`、`approval_title`、`applicant_name`、`task_status`、`write_status`、`created_at`、`updated_at`
- `approval_attachments`：`id`、`task_id`、`file_name`、`file_type`、`file_path`、`download_status`、`created_at`
- `contract_parses`：`id`、`task_id`、`basic_info_json`、`clause_info_json`、`parse_status`、`parse_error`、`created_at`
- `review_rules`：`id`、`rule_code`、`rule_name`、`risk_level`、`rule_status`、`match_mode`、`match_text`、`suggestion_text`、`updated_at`
- `rule_hits`：`id`、`task_id`、`rule_id`、`evidence_text`、`evidence_position`、`hit_status`、`created_at`
- `review_results`：`id`、`task_id`、`overall_risk_level`、`summary_text`、`focus_points_json`、`comment_text`、`created_at`
- `comment_logs`：`id`、`task_id`、`write_status`、`write_response_text`、`created_at`
- `task_logs`：`id`、`task_id`、`log_level`、`log_type`、`log_content`、`created_at`

#### 2.4.10 工具接口要求
- `list_pending_contract_approvals(limit)`：拉取待处理审批单列表
- `get_contract_approval(instance_id)`：查询单个审批单详情
- `download_contract_attachment(instance_id, attachment_id, file_name)`：下载合同附件并返回本地路径
- `parse_contract_document(document_id)`：解析合同文档并返回结构化字段
- `run_contract_rules(case_id)`：执行规则审查并返回命中结果和风险结论
- `save_review_result(case_id, overall_risk_level, summary_text, focus_points_json, comment_text)`：保存审查结果
- `write_approval_comment(instance_id, review_id)`：将审查意见写回审批评论区

#### 2.4.11 输出结果格式
- `总风险等级`：低、中、高
- `命中规则列表`：数组，每项至少包含规则名称、风险等级、命中证据、位置、建议
- `中文摘要`：对整体合同风险的总结
- `审批关注点`：数组，列出审批人需要重点确认的事项
- `回写内容`：最终写入评论区的文本内容

#### 2.4.12 验收标准
- 能通过工具服务拉取待处理审批单并按唯一业务标识去重
- 能通过工具服务下载合同附件并保存附件记录
- 能解析文档和图片扫描件，提取主要字段
- 能执行规则审查并返回命中证据和风险等级
- 能保存审查结果并将结果写回评论区
- 异常任务能够进入阻塞状态并支持重试
- 能完成待办拉取、附件解析、规则审查、结果入库、评论回写的完整闭环演示

### 2.5 自动化安全评估系统
- 项目目标：实现一个面向源码项目的自动化安全评估系统，支持项目接入、隔离分析、漏洞发现、漏洞验证、攻击路径整理和报告输出
- 交付形式：提供可运行前端、后端、数据库初始化脚本、示例项目、接口说明和完整演示链路

#### 2.5.1 开发范围
- 项目能力：项目创建、项目删除、任务启动、任务停止
- 执行能力：隔离环境管理、源码读取、目录遍历、关键字搜索、命令执行
- 结果能力：漏洞保存、攻击路径保存、报告输出、日志保存、阶段状态保存、实时监控

#### 2.5.2 用户角色
- `普通使用者`：创建评估项目、启动任务、查看漏洞、查看攻击路径、查看报告
- `管理员`：初始化系统、维护运行配置、维护隔离环境、查看资源消耗和系统日志

#### 2.5.3 页面需求
- `登录页`：输入用户名和密码，登录成功后进入项目列表页
- `项目列表页`：展示项目名称、源码来源、项目状态、最近启动时间、最近完成时间
- `项目创建页`：填写项目名称、源码路径或仓库地址、任务说明、隔离环境类型
- `项目详情页`：展示项目基本信息、阶段状态、漏洞数量、攻击路径数量、报告状态
- `实时监控页`：展示当前阶段、角色执行状态、聊天消息、日志滚动、资源消耗
- `漏洞列表页`：展示漏洞编号、漏洞标题、风险等级、验证状态、文件位置
- `攻击路径页`：展示攻击路径编号、关联漏洞、利用顺序、最终影响
- `报告页`：展示完整安全评估报告，支持下载
- `系统配置页`：展示隔离环境配置、默认超时时间、并发数、保留天数

#### 2.5.4 项目状态与执行阶段
- 项目状态至少包含 `created`、`running`、`completed`、`failed`、`stopped`
- 执行阶段至少包含 `environment_scan`、`code_analysis`、`vulnerability_verify`、`report_generate`、`done`
- 角色执行状态至少包含 `idle`、`running`、`success`、`failed`
- 启动任务后，必须先准备隔离环境，再进入阶段执行
- 停止任务后，当前阶段不再继续执行，但已经保存的数据保留
- 项目删除时，同时删除关联的漏洞记录、攻击路径记录、聊天消息、日志和资源记录

#### 2.5.5 角色分工要求
- 至少提供 6 类执行角色，覆盖通用处理、环境检查、代码分析、漏洞验证、报告整理、运维辅助
- 每类角色都要保存独立任务记录，包括接收的任务内容、开始时间、结束时间、执行结果、错误信息
- 角色执行结果必须能回溯到所属项目和所属阶段

#### 2.5.6 前端模块要求
- `项目模块`：处理项目创建、列表展示、详情查看、删除
- `监控模块`：处理实时状态、日志滚动、资源消耗展示
- `漏洞模块`：处理漏洞列表、详情展示、筛选
- `攻击路径模块`：处理路径列表、详情展示、关联漏洞展示
- `报告模块`：处理报告预览和下载
- `配置模块`：处理系统配置读取和更新

#### 2.5.7 后端模块要求
- `认证模块`：负责系统初始化、登录校验、登录态识别
- `项目模块`：负责项目创建、删除、查询、状态更新
- `隔离环境模块`：负责创建、启动、停止、销毁隔离环境
- `调度模块`：负责阶段推进、角色任务分发、状态汇总
- `漏洞模块`：负责漏洞记录保存、状态更新、证据保存
- `路径模块`：负责攻击路径保存和漏洞关联
- `报告模块`：负责汇总结果并生成最终报告
- `监控模块`：负责实时消息、日志记录、资源消耗记录

#### 2.5.8 数据表要求
- `users`：`id`、`username`、`password_hash`、`role`、`status`、`created_at`
- `projects`：`id`、`project_name`、`source_type`、`source_path`、`task_content`、`project_status`、`created_by`、`created_at`、`updated_at`
- `runtime_stages`：`id`、`project_id`、`stage_name`、`stage_status`、`started_at`、`finished_at`、`error_message`
- `worker_tasks`：`id`、`project_id`、`stage_id`、`worker_role`、`task_content`、`task_status`、`result_summary`、`started_at`、`finished_at`
- `vulnerabilities`：`id`、`project_id`、`vuln_code`、`vuln_title`、`risk_level`、`file_path`、`condition_text`、`evidence_text`、`verify_status`、`reproduce_steps_text`、`verify_code_text`、`created_at`
- `attack_paths`：`id`、`project_id`、`path_code`、`path_title`、`path_summary`、`final_impact_text`、`created_at`
- `attack_path_items`：`id`、`path_id`、`vuln_id`、`step_order`、`step_text`
- `chat_messages`：`id`、`project_id`、`worker_role`、`message_type`、`message_text`、`created_at`
- `runtime_logs`：`id`、`project_id`、`stage_id`、`worker_task_id`、`log_level`、`log_content`、`created_at`
- `resource_usages`：`id`、`project_id`、`cpu_usage`、`memory_usage`、`token_count`、`recorded_at`
- `reports`：`id`、`project_id`、`report_markdown`、`report_html`、`report_file_path`、`created_at`

#### 2.5.9 隔离环境与文件存储要求
- 每个项目必须绑定独立隔离环境编号
- 源码目录以只读形式挂载到隔离环境
- 日志文件按 `runtime_logs/{project_id}/` 存储
- 报告文件按 `reports/{project_id}/` 存储
- 临时执行文件按 `workspace/{project_id}/` 存储
- 删除项目时，同时删除日志目录、报告目录和临时目录

#### 2.5.10 接口要求
- `POST /api/system/init`：初始化管理员账户
- `POST /api/system/login`：请求字段 `username`、`password`
- `POST /api/projects`：请求字段 `project_name`、`source_type`、`source_path`、`task_content`
- `GET /api/projects`：查询项目列表
- `GET /api/projects/{project_id}`：查询项目详情
- `POST /api/projects/{project_id}/start`：启动评估任务
- `POST /api/projects/{project_id}/stop`：停止评估任务
- `DELETE /api/projects/{project_id}`：删除项目
- `GET /api/projects/{project_id}/stages`：查询阶段状态
- `GET /api/projects/{project_id}/workers`：查询角色执行状态
- `GET /api/projects/{project_id}/vulnerabilities`：查询漏洞列表
- `GET /api/projects/{project_id}/attack-paths`：查询攻击路径列表
- `GET /api/projects/{project_id}/report`：查询最终报告
- `GET /api/projects/{project_id}/logs`：查询运行日志
- `GET /api/projects/{project_id}/resources`：查询资源消耗
- `WS /api/projects/{project_id}/stream`：实时订阅状态、日志、消息、资源变化

#### 2.5.11 实时消息类型
- `project_status`：字段至少包含 `project_id`、`project_status`
- `stage_status`：字段至少包含 `project_id`、`stage_name`、`stage_status`
- `worker_status`：字段至少包含 `worker_task_id`、`worker_role`、`task_status`
- `chat_message`：字段至少包含 `worker_role`、`message_text`
- `runtime_log`：字段至少包含 `log_level`、`log_content`
- `resource_usage`：字段至少包含 `cpu_usage`、`memory_usage`、`token_count`
- `vulnerability_found`：字段至少包含 `vuln_id`、`vuln_title`、`risk_level`
- `report_ready`：字段至少包含 `project_id`、`report_id`

#### 2.5.12 输出结果格式
- `漏洞列表`：数组，每项至少包含漏洞编号、漏洞标题、风险等级、文件位置、验证状态
- `漏洞详情`：至少包含影响说明、触发条件、证据内容、复现步骤、验证代码
- `攻击路径`：至少包含路径编号、关联漏洞、利用顺序、最终影响
- `最终报告`：至少包含项目概述、执行过程、漏洞汇总、攻击路径、风险结论、修复建议

#### 2.5.13 验收标准
- 能初始化系统并完成登录
- 能创建项目并成功接入源码目录或仓库地址
- 能启动评估任务并看到阶段状态变化
- 能在实时监控页看到日志、角色状态、消息和资源变化
- 能生成漏洞记录、攻击路径和最终报告
- 能停止任务、删除项目并保留必要操作结果
- 能完成一个示例项目从接入、分析、验证到报告输出的完整演示

### 2.6 电商运营助手
- 项目目标：实现一个面向中小电商团队的运营工作台，围绕单个商品完成商品建档、竞品补充、商品诊断、主图方案、视频脚本、素材生成、投放建议、经营数据回填和复盘报告的完整闭环
- 交付形式：提供可运行前端、后端、数据库初始化脚本、示例数据、接口说明、导入模板和完整演示链路

#### 2.6.1 开发范围
- 基础能力：登录鉴权、角色权限、店铺管理、平台账号占位、导入中心、演示数据
- 运营能力：商品管理、竞品管理、商品诊断、主图方案、视频脚本、素材库、推广链接、投放建议、投放实验、经营数据录入、复盘报告
- 商品数据能力：多平台商品抽象、SKU 和库存管理、库存流水、库存预警、平台商品映射
- 异步任务能力：素材生成任务、图片和视频生成任务、失败重试、取消、超时处理、任务事件日志

#### 2.6.2 用户角色与权限
- `管理员`：可管理用户、管理店铺、配置平台账号、编辑商品、确认投放建议、查看所有数据
- `运营人员`：可编辑商品、竞品、方案、素材、经营数据，可确认投放建议，但不能管理用户
- `查看人员`：只能查看工作台、商品、报告和统计结果，不能执行新增、编辑、删除、确认类操作
- 所有写操作都必须做登录校验和角色校验
- 投放确认、实验状态更新、用户管理必须做单独权限校验

#### 2.6.3 业务闭环要求
- 业务主线必须覆盖 `建店建档 -> 录入商品 -> 补充竞品 -> 生成诊断 -> 生成主图方案 -> 生成视频脚本 -> 生成素材 -> 人工审核素材 -> 生成投放建议 -> 人工确认 -> 回填经营数据 -> 生成复盘报告`
- 所有诊断、方案、素材、投放建议和复盘都必须挂在具体商品下，不能脱离商品单独存在
- 系统必须支持从复盘结果回到诊断环节，形成下一轮优化闭环
- 第一版重点做单品运营闭环，同时页面和数据模型需要支持店铺维度和多平台扩展

#### 2.6.4 页面需求
- `登录页`：输入用户名和密码，登录成功进入首页工作台
- `首页工作台`：展示店铺卡片、平台筛选、商品池入口、低库存提示、创建店铺、创建商品、导入中心入口、演示数据入口
- `店铺列表页`：展示店铺名称、平台、商品数量、库存预警数量、负责人、更新时间
- `店铺详情页`：展示店铺基本信息、店铺商品列表、库存概览、最近库存流水、低库存商品、平台账号占位、库存建议
- `导入中心页`：展示商品导入、SKU 库存导入、经营数据导入、平台字段映射导入，支持模板下载、导入预览、错误行提示、部分成功导入
- `商品列表页`：展示商品名称、平台、类目、价格、店铺归属、状态、最近诊断时间、最近复盘时间
- `商品详情页`：至少包含 `商品&竞品`、`诊断`、`主图`、`视频`、`任务`、`素材库`、`链接`、`投放`、`复盘` 九个标签页
- `商品&竞品`：编辑商品基础信息、平台映射、SKU、库存、竞品、公开链接解析任务、竞品监控
- `诊断页`：生成、编辑和保存商品诊断
- `主图页`：生成主图方案、编辑方案、标记草稿/选中/归档、提交图片生成任务
- `视频页`：生成视频脚本、编辑脚本、标记草稿/选中/归档、提交视频生成任务
- `任务页`：展示生成任务状态、开始时间、结束时间、失败原因、事件时间线、重试按钮、取消按钮
- `素材库页`：展示图片和视频素材，支持审核状态、版本、使用场景、评分、标签、备注管理
- `链接页`：生成推广链接建议、创建推广链接、展示追踪参数和点击统计
- `投放页`：生成投放建议、人工确认或驳回、生成投放实验计划、更新实验状态
- `复盘页`：录入经营数据、导入经营数据、生成复盘报告、查看下一步动作建议
- `设置页`：管理模型配置、用户角色、系统参数、任务队列配置

#### 2.6.5 状态与业务规则
- 商品状态至少包含 `draft`、`active`、`inactive`
- 素材方案状态至少包含 `draft`、`selected`、`archived`
- 生成任务状态至少包含 `pending`、`running`、`succeeded`、`failed`、`cancelled`、`timeout`
- 投放建议确认状态至少包含 `pending`、`confirmed`、`rejected`
- 投放实验状态至少包含 `draft`、`confirmed`、`running`、`finished`、`cancelled`
- 平台账号只保存安全元数据和授权状态，不保存明文密码、明文 token 或 cookie
- 投放相关能力只能生成建议和实验计划，不能直接执行投放、扣费、预算修改
- 公开链接解析只做有限信息提取，不允许实现绕过登录、验证码或平台风控
- 经营数据允许手动录入和文件导入，导入失败时必须返回错误行和失败原因

#### 2.6.6 前端模块要求
- `认证模块`：处理登录、退出、登录态保持、页面访问校验
- `工作台模块`：处理店铺卡片、平台筛选、商品池入口、演示数据入口
- `店铺模块`：处理店铺新增、编辑、详情、库存概览、平台账号信息
- `商品模块`：处理商品列表、商品详情、商品基础信息、平台映射
- `SKU 与库存模块`：处理 SKU 新增、编辑、启停用、库存调整、库存流水展示
- `竞品模块`：处理竞品录入、竞品列表、公开链接解析任务、竞品监控
- `诊断模块`：处理商品诊断生成、编辑、保存
- `方案模块`：处理主图方案和视频脚本的生成、编辑、状态切换
- `任务模块`：处理生成任务列表、自动刷新、失败重试、取消任务、超时展示
- `素材模块`：处理素材库展示、审核状态、版本、评分、标签、备注
- `链接模块`：处理推广链接生成、创建、点击统计展示
- `投放模块`：处理投放建议、人工确认、驳回、实验计划管理
- `复盘模块`：处理经营数据录入、批量导入、复盘报告展示
- `导入模块`：处理模板下载、导入预览、错误行提示、导入结果回显
- `设置模块`：处理模型配置、用户管理、任务配置

#### 2.6.7 后端模块要求
- `认证模块`：负责登录、当前用户识别、角色权限校验
- `用户模块`：负责用户创建、用户更新、角色管理、状态管理
- `店铺模块`：负责店铺、平台账号、店铺详情、库存建议
- `商品模块`：负责商品新增、编辑、查询、平台映射
- `SKU 与库存模块`：负责 SKU 管理、库存记录、库存流水、库存预警
- `竞品模块`：负责竞品录入、竞品监控、公开链接解析任务
- `诊断模块`：负责结构化商品诊断生成和保存
- `方案模块`：负责主图方案、视频脚本、标题方案等内容方案生成与保存
- `任务模块`：负责图片和视频生成任务入队、执行、重试、取消、超时扫描
- `素材模块`：负责素材落库、素材同步、素材审核字段更新
- `链接模块`：负责推广链接建议、链接创建、点击记录
- `投放模块`：负责投放建议生成、人工确认记录、投放实验计划
- `经营分析模块`：负责经营数据录入、批量导入、复盘报告生成
- `工作区模块`：负责模板下载、演示数据创建、定时任务入口

#### 2.6.8 数据表要求
- `users`：`id`、`username`、`display_name`、`password_hash`、`role`、`status`、`last_login_at`、`created_at`、`updated_at`
- `stores`：`id`、`store_name`、`platform`、`external_store_id`、`owner_name`、`remark`、`created_at`、`updated_at`
- `platform_accounts`：`id`、`store_id`、`platform`、`account_name`、`auth_status`、`auth_meta_json`、`remark`、`created_at`、`updated_at`
- `products`：`id`、`store_id`、`name`、`platform`、`category`、`price`、`cost`、`target_audience`、`selling_points`、`product_url`、`images_json`、`status`、`created_at`、`updated_at`
- `platform_product_mappings`：`id`、`product_id`、`platform`、`platform_product_id`、`platform_sku_id`、`mapping_status`、`raw_payload_json`、`created_at`
- `product_skus`：`id`、`product_id`、`sku_code`、`sku_name`、`spec_json`、`price`、`cost`、`status`、`platform_sku_id`、`created_at`、`updated_at`
- `inventory_items`：`id`、`sku_id`、`stock_qty`、`locked_qty`、`warning_threshold`、`location_text`、`updated_at`
- `inventory_movements`：`id`、`sku_id`、`movement_type`、`change_qty`、`before_qty`、`after_qty`、`reason_text`、`reference_type`、`reference_id`、`created_at`
- `competitors`：`id`、`product_id`、`name`、`platform`、`url`、`price`、`sales_hint`、`title`、`main_image`、`selling_points`、`review_keywords`、`created_at`、`updated_at`
- `competitor_monitors`：`id`、`competitor_id`、`monitor_status`、`interval_minutes`、`next_run_at`、`created_at`
- `competitor_monitor_snapshots`：`id`、`monitor_id`、`price`、`sales_hint`、`selling_points`、`raw_payload_json`、`created_at`
- `public_link_parse_tasks`：`id`、`product_id`、`source_url`、`task_status`、`attempts`、`result_json`、`error_message`、`created_at`、`updated_at`
- `product_diagnoses`：`id`、`product_id`、`source_type`、`positioning`、`price_band`、`audience_insights`、`pain_points`、`selling_point_analysis`、`risks`、`recommendations`、`raw_output`、`created_at`、`updated_at`
- `creative_plans`：`id`、`product_id`、`plan_type`、`title`、`content_json`、`rationale_text`、`status`、`created_at`、`updated_at`
- `generation_jobs`：`id`、`product_id`、`creative_plan_id`、`job_kind`、`job_status`、`attempts`、`max_attempts`、`locked_at`、`locked_by`、`next_run_at`、`result_json`、`error_message`、`started_at`、`finished_at`
- `generation_job_events`：`id`、`job_id`、`event_type`、`event_message`、`created_at`
- `generated_assets`：`id`、`product_id`、`creative_plan_id`、`asset_type`、`asset_url`、`model_name`、`width`、`height`、`duration_sec`、`review_status`、`version_no`、`usage_scene`、`score`、`tags_json`、`remark`、`created_at`
- `promotion_links`：`id`、`product_id`、`link_name`、`target_url`、`tracking_code`、`utm_json`、`status`、`click_count`、`scene_text`、`created_at`
- `promotion_link_clicks`：`id`、`promotion_link_id`、`clicked_at`、`client_ip`、`user_agent`
- `ad_recommendations`：`id`、`product_id`、`summary_text`、`objective_text`、`audience_segments_json`、`budget_plan_json`、`creative_tests_json`、`bid_strategy_json`、`risk_controls_json`、`next_steps_json`、`confirm_status`、`confirmed_by`、`confirmed_at`、`confirm_remark`、`created_at`
- `ad_experiments`：`id`、`product_id`、`related_asset_id`、`related_link_id`、`experiment_name`、`target_text`、`audience_text`、`budget_amount`、`success_metric_text`、`hypothesis_text`、`experiment_status`、`created_at`、`updated_at`
- `performance_records`：`id`、`product_id`、`creative_plan_id`、`generated_asset_id`、`promotion_link_id`、`experiment_id`、`period_start`、`period_end`、`impressions`、`clicks`、`ctr`、`conversions`、`conversion_rate`、`spend`、`revenue`、`roi`、`notes`、`created_at`
- `review_reports`：`id`、`product_id`、`period_start`、`period_end`、`summary_text`、`insights_json`、`next_actions_json`、`created_at`

#### 2.6.9 AI 输出与异步任务要求
- 商品诊断输出必须结构化保存，至少包含 `商品定位`、`价格带分析`、`目标人群洞察`、`用户痛点`、`卖点分析`、`风险点`、`优化建议`
- 主图方案输出至少包含 3 个方向，每个方向要有 `方案标题`、`画面结构`、`核心文案`、`突出卖点`、`方案理由`
- 视频脚本输出至少包含 3 条脚本，每条脚本要有 `开头钩子`、`镜头分镜`、`口播文案`、`转化引导`
- 投放建议输出至少包含 `策略摘要`、`本轮目标`、`人群建议`、`预算建议`、`素材测试建议`、`出价策略`、`风险控制`、`下一步动作`
- 复盘报告输出至少包含 `周期摘要`、`核心洞察`、`问题判断`、`下一步动作`
- 所有 AI 输出都必须允许用户编辑保存，不能只展示原始文本
- 图片和视频生成必须通过异步任务执行，不能阻塞普通页面请求
- 异步任务必须支持 `入队`、`运行中`、`成功`、`失败`、`重试`、`取消`、`超时` 这些事件节点

#### 2.6.10 接口要求
- `POST /api/v1/auth/login`：用户登录并返回访问令牌
- `GET /api/v1/auth/me`：查询当前登录用户
- `GET /api/v1/users`、`POST /api/v1/users`、`PATCH /api/v1/users/{user_id}`：用户管理
- `GET /api/v1/stores`、`POST /api/v1/stores`：店铺列表和创建
- `GET /api/v1/stores/{store_id}`、`PATCH /api/v1/stores/{store_id}`：店铺详情和更新
- `POST /api/v1/stores/{store_id}/platform-accounts`：创建平台账号占位
- `POST /api/v1/stores/{store_id}/platform-accounts/{account_id}/authorization/start`：生成授权占位信息
- `POST /api/v1/stores/{store_id}/platform-accounts/{account_id}/authorization/callback-placeholder`：保存授权元数据
- `POST /api/v1/stores/{store_id}/inventory-advice/generate`：生成库存建议
- `GET /api/v1/products`、`POST /api/v1/products`：商品列表和创建
- `GET /api/v1/products/{product_id}`、`PATCH /api/v1/products/{product_id}`：商品详情和更新
- `POST /api/v1/products/{product_id}/platform-mappings`：商品平台映射
- `POST /api/v1/products/{product_id}/competitors`：录入竞品
- `POST /api/v1/products/{product_id}/competitors/import-url-tasks`：创建公开链接解析任务
- `POST /api/v1/products/{product_id}/link-parse-tasks/{task_id}/run`：运行公开链接解析任务
- `POST /api/v1/products/{product_id}/competitors/{competitor_id}/monitor`：创建竞品监控
- `POST /api/v1/products/{product_id}/diagnoses/generate`：生成商品诊断
- `PATCH /api/v1/products/{product_id}/diagnoses/{diagnosis_id}`：编辑商品诊断
- `POST /api/v1/products/{product_id}/creative-plans/main-images/generate`：生成主图方案
- `POST /api/v1/products/{product_id}/creative-plans/video-scripts/generate`：生成视频脚本
- `PATCH /api/v1/products/{product_id}/creative-plans/{creative_plan_id}`：编辑方案
- `POST /api/v1/products/{product_id}/creative-plans/{creative_plan_id}/images/generate`：创建图片生成任务
- `POST /api/v1/products/{product_id}/creative-plans/{creative_plan_id}/videos/generate`：创建视频生成任务
- `POST /api/v1/products/{product_id}/generation-jobs/{job_id}/retry`：重试失败任务
- `POST /api/v1/products/{product_id}/generation-jobs/{job_id}/cancel`：取消任务
- `POST /api/v1/workspace/generation-jobs/sweep-timeouts`：扫描超时任务
- `POST /api/v1/products/{product_id}/assets/sync`：同步素材
- `PATCH /api/v1/products/{product_id}/creative-plans/{creative_plan_id}/assets`：更新素材审核字段
- `POST /api/v1/products/{product_id}/promotion-links/generate`：生成推广链接建议
- `POST /api/v1/products/{product_id}/promotion-links`：创建推广链接
- `GET /api/v1/r/{tracking_code}`：推广链接跳转并记录点击
- `POST /api/v1/products/{product_id}/ad-recommendations/generate`：生成投放建议
- `PATCH /api/v1/products/{product_id}/ad-recommendations/{recommendation_id}/confirmation`：人工确认或驳回投放建议
- `POST /api/v1/products/{product_id}/ad-experiments/generate`：生成投放实验计划
- `PATCH /api/v1/products/{product_id}/ad-experiments/{experiment_id}`：更新实验状态
- `POST /api/v1/products/{product_id}/performance-records`：录入经营数据
- `POST /api/v1/products/{product_id}/performance-records/import`：批量导入经营数据
- `POST /api/v1/products/{product_id}/review-reports/generate`：生成复盘报告
- `POST /api/v1/workspace/demo-data`：创建演示数据
- `GET /api/v1/workspace/templates/products`：下载商品导入模板
- `GET /api/v1/workspace/templates/sku-inventory`：下载 SKU 库存导入模板
- `GET /api/v1/workspace/templates/performance-records`：下载经营数据导入模板
- `POST /api/v1/workspace/competitor-monitors/run-due`：运行到期竞品监控

#### 2.6.11 导入、模板与演示数据要求
- 系统必须提供商品导入模板、SKU 库存导入模板、经营数据导入模板
- 导入时必须先做字段校验，再返回导入预览和错误行提示
- 允许部分成功导入，成功和失败结果都要分别统计
- 演示数据至少覆盖店铺、平台账号占位、商品、SKU、库存、竞品、主图方案、视频脚本、素材、推广链接、投放建议、投放实验、经营数据、复盘报告
- 新环境允许通过一个接口快速初始化演示数据，便于课程演示和验收

#### 2.6.12 合规边界
- 不保存明文平台密码、明文 token、明文 cookie
- 不实现绕过登录、验证码、风控或访问控制的数据抓取逻辑
- 不自动创建平台广告计划，不自动扣费，不直接执行预算变更
- 投放建议和实验计划必须由有权限的人员人工确认
- 数据采集优先使用平台开放接口、用户授权、文件导入和公开链接解析

#### 2.6.13 验收标准
- 能完成登录并根据角色显示不同权限
- 能创建店铺、创建商品、维护 SKU 和库存，并生成库存流水
- 能录入竞品并运行公开链接解析任务
- 能生成并编辑商品诊断、主图方案和视频脚本
- 能创建图片和视频生成任务，并在任务页看到状态、事件、重试和取消结果
- 能在素材库维护审核状态、版本、评分和标签
- 能生成推广链接建议并记录点击
- 能生成投放建议并完成人工确认或驳回
- 能录入或导入经营数据并生成复盘报告
- 能通过演示数据跑通一条完整的单品运营闭环演示

### 2.7 财务单据智能风险审核系统（真实需求，无源码）
- 项目目标：实现一个独立运行的财务单据智能风险审核系统，覆盖单据创建、附件管理、审批流转、风险分析、人工复核、报告导出和操作审计
- 交付形式：提供可运行前端、后端、数据库初始化脚本、文件存储模块、示例数据、接口说明和完整审核演示链路

#### 2.7.1 开发范围
- 系统能力：独立实现用户认证、角色权限、单据管理、附件管理、审批流程、规则配置、风险分析、报告管理和操作审计
- 基础能力：用户登录、角色授权、审核会话、操作日志、任务状态管理和系统参数配置
- 单据能力：单据创建、编辑、复制、查询、提交、撤回、作废、结构化字段管理和明细管理
- 审批能力：审批流程配置、审批任务生成、审批意见提交、退回修改、通过、驳回和状态追踪
- 附件能力：附件上传、下载、预览、删除、格式校验、访问控制和解析状态管理
- 解析能力：PDF、PNG 和 JPG 附件解析、OCR 识别、发票与合同关键字段提取、原文定位
- 分析能力：金额一致性校验、明细合计校验、开支合理性分析、市场价对比、消费异常识别、供应商风险分析
- 展示能力：风险概览、风险明细、证据对照、处理建议、人工复核记录

#### 2.7.2 单据与数据范围
- 支持 `对公付款单`、`预付款单`、`批量付款单`、`费用报销单`、`差旅报销单` 五类主要单据
- 结构化字段至少包含单据类型、单据编号、申请人、申请部门、预算部门、收款单位、收款账号、费用类别、支出金额、总金额、币种、申请日期和事由
- 对公付款单和预付款单补充合同编号、供应商名称、付款比例、付款条件和计划付款日期
- 批量付款单补充付款明细、收款对象、单笔金额、批次总金额和付款笔数
- 费用报销单补充费用明细、消费日期、消费地点、费用科目和报销金额
- 差旅报销单补充出差地点、出差起止日期、交通费、住宿费、餐费和补贴金额
- 非结构化附件至少包含发票、合同、行程单、付款依据和费用明细，支持 PDF、PNG、JPG 格式
- 保存附件解析文本、字段提取结果、页码、文本位置和识别置信度

#### 2.7.3 用户角色与权限
- `单据申请人`：创建和编辑单据、维护明细、上传附件、提交审批、撤回未处理单据和查看本人单据状态
- `审批人员`：发起单据分析、查看风险面板、查看证据、填写复核意见和提交审批结果
- `财务人员`：查看全部单据分析结果、维护审核规则、维护市场价参考数据和供应商风险信息
- `系统管理员`：维护用户、角色、权限、审批流程、模型配置和系统参数
- 所有单据查询、附件访问、风险分析和审批操作都必须校验登录状态与数据权限
- 审批结果、风险项处理和规则变更必须记录操作人、操作时间和变更内容

#### 2.7.4 页面需求
- `登录页`：输入用户名和密码，登录成功后进入审核工作台
- `单据管理页`：按单据编号、单据类型、申请人、部门、状态和日期查询单据，支持新建、复制、撤回和作废
- `单据编辑页`：维护单据基本信息、费用明细、付款明细和附件，支持保存草稿、提交审批和字段校验
- `审核工作台`：展示待审批单据数量、高中低风险数量、单据类型分布、最近分析任务和待人工复核事项
- `智能审核对话页`：输入单据类型和单据编号，通过多轮交互补充缺失信息并发起分析
- `单据详情页`：展示单据基本信息、费用明细、付款明细、附件列表、审批进度和状态记录
- `附件解析页`：展示附件原图或原文、识别文本、提取字段、置信度和字段来源位置
- `风险分析页`：展示整体风险等级、风险分类统计、风险项列表、证据对照和处理建议
- `金额核对面板`：对照单据总金额、明细合计、发票金额合计、合同金额和付款金额，并突出显示差异
- `供应商风险页`：展示供应商基本信息、历史付款、风险标签、黑名单状态和异常记录
- `审批流程配置页`：按单据类型、金额区间和部门维护审批节点、审批角色和节点顺序
- `规则配置页`：维护金额容差、费用标准、市场价区间、异常阈值和供应商风险规则
- `审核记录页`：查询历史分析报告、人工复核意见、审批结果和操作日志

#### 2.7.5 单据与审核流程
- 申请人创建单据并填写基本信息、费用明细或付款明细
- 申请人上传所需附件，系统校验字段完整性、明细合计和附件要求
- 申请人提交单据后，系统生成单据快照、审批实例和首个审批任务
- 系统解析 PDF、PNG、JPG 附件，提取发票、合同、行程单和付款依据中的关键字段
- 系统汇总单据字段、明细、附件解析结果、审核规则、市场价参考数据和供应商资料，生成风险分析结果
- 审批人员查看风险项、判断依据和证据位置，提交通过、退回或驳回结果
- 当前节点通过后，系统生成下一审批节点任务；最后一个节点通过后，单据状态变为已通过
- 退回单据允许申请人修改后重新提交，重新提交时生成新版本和新的分析任务
- 系统保存单据版本、状态变化、审批意见、风险项处理记录和操作日志

#### 2.7.6 多轮交互规则
- 用户首次输入未包含单据类型时，系统询问单据类型
- 用户首次输入未包含单据编号时，系统询问单据编号
- 用户提供的信息存在歧义或冲突时，系统展示候选项并请求确认
- 系统在会话中保存已确认的单据类型和单据编号，不重复询问已确认信息
- 信息完整后，系统按 `单据类型 + 单据编号` 查询系统内保存的单据、明细和附件
- 用户只能分析数据权限范围内的单据
- 智能分析模块汇总单据字段、明细、附件解析结果、审核规则、市场价参考数据和供应商资料，生成风险分析结果
- 系统保存风险项、判断依据、证据位置、风险等级、处理建议和分析时间
- 单据查询失败、附件读取失败或附件解析失败时，系统展示失败环节并允许重试

#### 2.7.7 风险分析规则
- `单据与发票金额一致性`：对比单据申请金额、报销金额与发票含税金额合计，输出差异金额和差异比例
- `明细与总金额一致性`：计算多项支出明细合计并与单据总金额对比，标记漏项、重复项和合计差异
- `合同与付款一致性`：对比合同主体、合同金额、付款条件、付款比例与当前付款信息
- `批量付款一致性`：对比付款笔数、各笔金额合计与批次总金额，识别重复收款账号和重复付款
- `费用标准合规性`：按费用类别、预算部门、职级、地区和日期对比企业费用标准
- `市场价格合理性`：按商品或服务名称、规格、地区和时间对比市场价参考区间，输出价格偏离比例
- `消费行为异常`：识别短期高频消费、同日重复报销、节假日异常消费、异地异常消费、拆单报销和历史金额突增
- `供应商风险`：识别供应商黑名单、资质异常、关联交易、历史履约异常、收款账号变更和集中付款异常
- `附件完整性`：检查必需附件、附件清晰度、关键字段完整性和单据主体一致性
- `重复票据风险`：按发票代码、发票号码、金额、开票日期和销售方识别重复提交
- 单项风险等级至少包含 `low`、`medium`、`high`，整体风险等级取最高单项风险并结合风险数量生成
- 金额差异、市场价格和异常行为结论必须展示计算值、对比值、规则阈值和数据来源

#### 2.7.8 前端模块要求
- `认证模块`：处理登录、退出、登录态保持和页面访问校验
- `单据编辑模块`：处理单据创建、字段编辑、明细维护、保存草稿、复制、提交、撤回和作废
- `工作台模块`：处理待审批统计、风险统计、最近任务和待复核事项展示
- `对话模块`：处理多轮消息、单据类型澄清、单据编号澄清和分析进度展示
- `单据查询模块`：处理单据筛选、列表展示、详情展示、版本记录和状态记录
- `附件模块`：处理附件上传、删除、下载、预览、解析文本、提取字段、证据定位和解析失败重试
- `审批模块`：处理审批任务列表、审批进度、审批意见、通过、退回和驳回操作
- `风险模块`：处理风险等级、风险分类、风险明细、判断依据和处理建议展示
- `金额核对模块`：处理单据金额、明细金额、发票金额、合同金额和付款金额对照
- `供应商模块`：处理供应商资料、历史交易和风险标签展示
- `流程配置模块`：处理审批流程、审批节点、适用条件和审批角色配置
- `规则模块`：处理金额容差、费用标准、市场价区间和风险阈值配置
- `审核记录模块`：处理人工复核意见、风险项确认、审批结果和操作记录展示

#### 2.7.9 后端模块要求
- `认证与权限模块`：负责登录、当前用户识别、角色权限和单据数据权限校验
- `会话模块`：负责审核会话、消息记录、槽位状态和多轮上下文管理
- `单据模块`：负责单据创建、编辑、复制、查询、提交、撤回、作废、版本保存和状态流转
- `明细模块`：负责费用明细、付款明细、金额计算和明细校验
- `附件模块`：负责附件上传、保存、删除、下载、格式校验、访问控制和解析任务创建
- `审批流程模块`：负责流程定义、适用条件匹配、审批实例创建、节点流转和审批任务管理
- `文档解析模块`：负责 PDF 文本提取、图片 OCR、文档分类、字段提取和证据定位
- `规则引擎模块`：负责金额容差、费用标准、市场价区间和异常阈值校验
- `智能分析模块`：负责聚合结构化数据、附件内容、规则结果、市场价和供应商信息并生成风险结论
- `供应商模块`：负责供应商档案、风险标签、黑名单和历史交易查询
- `报告模块`：负责风险结果保存、可视化数据生成和报告导出
- `审核模块`：负责人工复核意见、风险项处理状态和最终审批结果记录
- `日志模块`：负责接口调用、附件访问、规则变更、分析任务和审批操作审计

#### 2.7.10 数据表要求
- `users`：`id`、`username`、`display_name`、`password_hash`、`status`、`created_at`、`updated_at`
- `roles`：`id`、`role_code`、`role_name`、`status`、`created_at`、`updated_at`
- `permissions`：`id`、`permission_code`、`permission_name`、`resource_type`、`action_type`
- `user_roles`：`id`、`user_id`、`role_id`
- `role_permissions`：`id`、`role_id`、`permission_id`
- `review_sessions`：`id`、`user_id`、`document_type`、`document_no`、`session_status`、`created_at`、`updated_at`
- `session_messages`：`id`、`session_id`、`role`、`content`、`message_type`、`created_at`
- `financial_documents`：`id`、`document_type`、`document_no`、`applicant_id`、`applicant_department`、`budget_department`、`payee_name`、`payee_account`、`expense_category`、`total_amount`、`currency`、`apply_date`、`reason_text`、`document_status`、`current_version`、`created_at`、`updated_at`
- `document_versions`：`id`、`document_id`、`version_no`、`document_snapshot_json`、`created_by`、`created_at`
- `document_line_items`：`id`、`document_id`、`item_type`、`item_name`、`expense_date`、`expense_location`、`quantity`、`unit_price`、`amount`、`remark`
- `document_attachments`：`id`、`document_id`、`document_version`、`file_name`、`file_type`、`file_size`、`file_path`、`file_hash`、`storage_status`、`parse_status`、`created_at`
- `attachment_parse_results`：`id`、`attachment_id`、`document_category`、`full_text`、`fields_json`、`evidence_positions_json`、`confidence`、`created_at`
- `invoice_records`：`id`、`attachment_id`、`invoice_code`、`invoice_no`、`seller_name`、`buyer_name`、`invoice_date`、`amount_excluding_tax`、`tax_amount`、`amount_including_tax`、`currency`
- `approval_workflows`：`id`、`workflow_name`、`document_type`、`match_conditions_json`、`status`、`created_at`、`updated_at`
- `approval_workflow_nodes`：`id`、`workflow_id`、`node_name`、`node_order`、`approver_role`、`approval_mode`、`created_at`
- `approval_instances`：`id`、`workflow_id`、`document_id`、`document_version`、`instance_status`、`current_node_id`、`started_at`、`finished_at`
- `approval_tasks`：`id`、`instance_id`、`node_id`、`approver_id`、`task_status`、`review_comment`、`created_at`、`processed_at`
- `document_status_logs`：`id`、`document_id`、`from_status`、`to_status`、`operator_id`、`remark`、`created_at`
- `analysis_tasks`：`id`、`session_id`、`document_id`、`task_status`、`current_step`、`started_at`、`finished_at`、`error_message`
- `risk_findings`：`id`、`task_id`、`risk_type`、`risk_level`、`risk_title`、`description`、`actual_value_json`、`reference_value_json`、`threshold_json`、`evidence_json`、`suggestion_text`、`review_status`、`created_at`
- `review_reports`：`id`、`task_id`、`document_id`、`overall_risk_level`、`risk_summary_json`、`amount_comparison_json`、`recommendation`、`report_markdown`、`created_at`
- `market_price_references`：`id`、`item_name`、`specification`、`region`、`price_min`、`price_max`、`currency`、`source_name`、`effective_date`
- `supplier_profiles`：`id`、`supplier_code`、`supplier_name`、`credit_status`、`blacklist_status`、`risk_tags_json`、`bank_accounts_json`、`updated_at`
- `manual_reviews`：`id`、`report_id`、`reviewer_id`、`review_result`、`review_comment`、`reviewed_at`
- `audit_logs`：`id`、`user_id`、`action_type`、`resource_type`、`resource_id`、`detail_json`、`created_at`

#### 2.7.11 接口要求
- `POST /api/v1/auth/login`：用户登录并返回访问令牌
- `GET /api/v1/auth/me`：查询当前登录用户
- `POST /api/v1/documents`：创建单据
- `GET /api/v1/documents`：按单据类型、申请人、部门、状态和日期查询单据列表
- `GET /api/v1/documents/{document_id}`：查询单据详情、明细、附件、版本和审批进度
- `PATCH /api/v1/documents/{document_id}`：编辑草稿或退回状态的单据
- `POST /api/v1/documents/{document_id}/copy`：复制单据并生成新草稿
- `POST /api/v1/documents/{document_id}/submit`：提交单据并创建审批实例和分析任务
- `POST /api/v1/documents/{document_id}/withdraw`：撤回未处理的单据
- `POST /api/v1/documents/{document_id}/void`：作废符合条件的单据
- `POST /api/v1/documents/{document_id}/line-items`：新增单据明细
- `PATCH /api/v1/documents/{document_id}/line-items/{line_item_id}`：更新单据明细
- `DELETE /api/v1/documents/{document_id}/line-items/{line_item_id}`：删除单据明细
- `POST /api/v1/documents/{document_id}/attachments`：上传单据附件
- `GET /api/v1/documents/{document_id}/attachments/{attachment_id}`：下载或预览单据附件
- `DELETE /api/v1/documents/{document_id}/attachments/{attachment_id}`：删除单据附件
- `POST /api/v1/review-sessions`：创建审核会话
- `POST /api/v1/review-sessions/{session_id}/messages`：发送消息并返回澄清问题或分析任务信息
- `GET /api/v1/review-sessions/{session_id}/messages`：查询会话历史消息
- `POST /api/v1/documents/{document_id}/attachments/{attachment_id}/parse`：创建附件解析任务
- `POST /api/v1/documents/{document_id}/analysis`：创建单据风险分析任务
- `GET /api/v1/analysis-tasks/{task_id}`：查询分析任务状态和当前步骤
- `GET /api/v1/analysis-tasks/{task_id}/findings`：查询风险项列表
- `GET /api/v1/analysis-tasks/{task_id}/report`：查询风险报告和面板数据
- `GET /api/v1/documents/{document_id}/amount-comparison`：查询金额核对结果
- `GET /api/v1/suppliers/{supplier_code}/risks`：查询供应商风险信息
- `PATCH /api/v1/risk-findings/{finding_id}/review-status`：更新风险项人工复核状态
- `POST /api/v1/review-reports/{report_id}/manual-reviews`：提交人工复核意见和审批结果
- `GET /api/v1/review-reports/{report_id}/export`：导出风险审核报告
- `GET /api/v1/approval-tasks`：查询当前用户的审批任务
- `POST /api/v1/approval-tasks/{task_id}/approve`：通过审批任务
- `POST /api/v1/approval-tasks/{task_id}/return`：退回审批任务
- `POST /api/v1/approval-tasks/{task_id}/reject`：驳回审批任务
- `GET /api/v1/approval-workflows`、`POST /api/v1/approval-workflows`、`PATCH /api/v1/approval-workflows/{workflow_id}`：查询、创建和更新审批流程
- `GET /api/v1/rules`、`POST /api/v1/rules`、`PATCH /api/v1/rules/{rule_id}`：查询、创建和更新审核规则

#### 2.7.12 状态与实时消息
- 单据状态至少包含 `draft`、`pending_review`、`reviewing`、`returned`、`approved`、`rejected`、`withdrawn`、`voided`
- 审批实例状态至少包含 `pending`、`running`、`approved`、`returned`、`rejected`、`cancelled`
- 审批任务状态至少包含 `pending`、`approved`、`returned`、`rejected`、`cancelled`
- 附件存储状态至少包含 `uploading`、`stored`、`failed`
- 附件解析状态至少包含 `pending`、`parsing`、`succeeded`、`failed`、`manual_review`
- 分析任务状态至少包含 `queued`、`querying_document`、`loading_attachments`、`parsing_attachments`、`analyzing`、`succeeded`、`failed`、`cancelled`
- 风险项复核状态至少包含 `pending`、`confirmed`、`dismissed`
- `document_status`：字段至少包含 `document_id`、`document_status`、`current_version`
- `approval_status`：字段至少包含 `document_id`、`instance_id`、`node_id`、`task_status`
- `slot_required`：字段至少包含 `session_id`、`slot_name`、`question_text`、`candidate_values`
- `task_status`：字段至少包含 `task_id`、`task_status`、`current_step`、`progress`
- `attachment_status`：字段至少包含 `attachment_id`、`storage_status`、`parse_status`
- `risk_finding`：字段至少包含 `task_id`、`finding_id`、`risk_type`、`risk_level`、`risk_title`
- `report_ready`：字段至少包含 `task_id`、`report_id`、`overall_risk_level`
- `error`：字段至少包含 `task_id`、`error_code`、`error_message`
- `done`：字段至少包含 `task_id`、`finished_at`

#### 2.7.13 风险报告输出格式
- `单据摘要`：展示单据类型、单据编号、申请人、预算部门、收款单位、总金额和附件数量
- `整体风险`：展示整体风险等级、高中低风险数量和审核建议
- `金额核对`：展示单据总金额、明细合计、发票合计、合同金额、付款金额和各项差异
- `风险项列表`：每项包含风险类型、风险等级、风险描述、实际值、参考值、规则阈值和处理建议
- `证据列表`：每项包含来源类型、附件名称、页码、原文片段、结构化字段和识别置信度
- `供应商风险`：展示供应商风险标签、黑名单状态、历史异常和收款账号变化
- `处理建议`：至少包含 `建议通过`、`补充材料`、`人工复核`、`建议驳回` 四类
- `人工复核`：展示复核人、复核结论、复核意见和复核时间

#### 2.7.14 安全边界
- 系统独立保存和管理用户、单据、附件、审批、分析、报告与审计数据
- 密码必须使用安全哈希保存，访问令牌必须设置有效期和撤销机制
- 单据字段、附件内容、供应商资料和分析结果按用户权限访问
- 附件上传和下载必须校验文件类型、文件大小、文件路径和访问权限
- 智能分析结果只作为审批辅助依据，最终审批结果由有权限的人员人工确认
- 所有金额对比、规则判断和风险结论都必须保留数据来源和审计记录

#### 2.7.15 验收标准
- 能完成用户登录、角色授权和数据权限校验
- 能创建五类单据，维护结构化字段、费用明细、付款明细和附件
- 能保存草稿、复制单据、提交审批、撤回、退回修改、重新提交和作废单据
- 能按审批流程生成任务、完成多节点审批并展示完整审批进度
- 能通过多轮交互补齐单据类型和单据编号，并保持已确认的会话信息
- 能从系统内查询五类单据的结构化字段、明细和附件
- 能上传、预览、下载并解析 PDF、PNG、JPG 附件，展示解析文本、提取字段和证据位置
- 能完成单据金额、明细合计、发票金额、合同金额和付款金额的交叉核对
- 能识别开支合理性、市场价偏离、消费异常、供应商风险、附件缺失和重复票据风险
- 能在可视化面板展示整体风险、风险项、金额差异、证据和处理建议
- 能提交人工复核意见和审批结果，并查询完整操作日志
- 能通过示例数据跑通 `单据创建 -> 明细维护 -> 附件上传 -> 提交审批 -> 附件解析 -> 风险分析 -> 审批处理 -> 报告导出` 完整链路

### 2.8 生产级 ETL-Agent 数据集成平台
- 项目目标：实现一个面向企业数据集成场景的生产级 ETL-Agent 控制面平台，结合 LangGraph 与不可绕过的 Agent Harness 内核，覆盖自然语言需求解析、受管异构元数据安全探查、结构化 EtlPlan 与 SeaTunnel HOCON 配置生成、确定性门禁校验、不可变制品版本化、四眼职责审批（Maker-Checker）、单次 Capability 令牌授权、Celery 异步调度与 SeaTunnel 执行、运行监督与质量契约分流闭环
- 交付形式：提供可运行 Vue 前端控制台、FastAPI 控制面服务、Celery 异步 Worker/Beat 服务、PostgreSQL/Redis 数据库脚本、SeaTunnel Zeta 集成配置、仿真评测 Benchmark 数据集、接口说明文档与完整业务演示链路

#### 2.8.1 开发范围
- 基础能力：用户认证鉴权、多租户项目管理、项目成员与角色分配、敏感凭据受管加密存储（SecretProvider / Vault KV v2）
- 数据源与元数据探查：异构数据库接入（MySQL、PostgreSQL、Oracle、Doris、ClickHouse）、文件与 API 数据源接入（CSV、Excel、JSON、Parquet、S3/MinIO、REST API）、只读元数据安全探查（Schema、字段类型、近似统计、脱敏样本采样）
- 智能生成与编排能力：自然语言需求解析、LangGraph 中断与 Checkpoint 恢复、多模型 Provider 适配（OpenAI 兼容接口 / DeepSeek / Qwen）、EtlPlan 与 SeaTunnel HOCON 候选生成、语法与 Schema 对齐校验、有限自动修复
- Harness 安全内核能力：策略决策点（PDP / P0-P3 风险评级）、`prepare → approve → commit` 三阶段执行协议、四眼原则独立审批（Maker-Checker）、Ed25519 签名短时单次 Capability 令牌、防重放 Guard、事务 Outbox 可靠投递、证据账本（Evidence Ledger）追加与防篡改
- 数据面执行与治理：Celery 异步任务执行、SeaTunnel 引擎对接、影子表与原子 Swap 发布、错误数据分流与质量契约（QualityContract）、运行监督（行数/字节/时长/放大比/拒绝率预算监控）、任务取消与回滚
- 评测与安全进化：分层仿真（L0 静态注入、L1 模拟故障、L2 真实链路）、版本化 Benchmark 自动化评测、受控安全进化闭环

#### 2.8.2 用户角色与职责权限
- `数据工程师（申请人 / Maker）`：登记数据源连接、发起元数据探查、输入自然语言需求、调试并生成 Pipeline 候选方案、提交不可变版本与审批申请
- `数据审批人（Checker 1）`：审查需求描述、Schema 映射、脱敏前后对比、质量规则与 DAG 结构，执行数据维度审批
- `安全审批人（Checker 2）`：审查资源范围、数据敏感分级、执行预算、Secret 引用与回滚方案，执行安全维度审批；管理 Benchmark 与安全进化
- `系统操作员`：在所有审批通过后执行 Commit 操作、触发受管执行、监控运行状态、执行任务暂停/取消/回滚与凭据轮转
- `审计人员`：查看执行历史、审计事件、签名证据账本、数据质量报告与 Benchmark 评测大盘
- 强制职责分离规则：申请人禁止自批，高风险操作禁止同一人占用多个审批职责槽

#### 2.8.3 页面需求
- `总览工作台`：展示当前项目连接数、Pipeline 数量、待审批门禁概况、运行成功率与核心资源分布
- `数据连接与资产页`：
  - 连接管理：维护数据库连接与文件数据源，支持连接测试与 Secret 密文保存
  - 元数据探查：展示数据表 Schema、字段类型、主键、近似统计指标与脱敏样本视图
  - 文件资产管理：上传与解析本地/对象存储文件，提取文件头与字段推断
- `Pipeline Studio（设计与生成工作台）`：
  - 需求输入区：输入自然语言业务需求、选择源端与目标端 Profile
  - Agent 对话与澄清区：展示 LangGraph 状态机执行步骤、需求澄清提问、交互表单与有限修复过程
  - 方案与配置审查区：展示生成的 EtlPlan 结构化设计、SeaTunnel HOCON 配置、DAG 拓扑图、字段映射 Diff 与脱敏前后结构对比
  - 版本与审批提交区：将通过门禁的方案冻结为不可变版本（SHA256 摘要），提交审批申请并配置调度策略
- `运行中心`：
  - 准备与审批面板：展示 Preparation 准备单、影响范围、回滚方案、审批人列表与审批决策入口
  - 执行与监控：展示 ExecutionRun 列表、实时运行状态、读取/写入/过滤行数、字节数、执行时长、吞吐量与实时日志
  - 质量与诊断报告：展示有效数据写入统计、错误表分流明细、错误码分布、质量契约命中与可解释根因诊断
  - 运维操作：提供任务取消、安全重跑、影子表回滚与清理入口
- `安全治理与 Benchmark 页`：
  - Benchmark 大盘：运行自动化 Benchmark 测试集，展示生成准确率、Schema 覆盖率、P0/P1 安全拦截率与延迟对比
  - 安全进化管理：展示 Prompt/策略改进候选、审查报告与小流量/影子授权开关

#### 2.8.4 核心协议与系统规则
- 控制面与数据面分离：FastAPI + LangGraph + Celery + PostgreSQL 负责控制面治理，Apache SeaTunnel 负责数据面搬运，控制面不直连搬运海量业务数据
- 三层状态隔离规则：
  - 对话状态（Conversation State）：记录自然语言交互历史
  - Agent 状态机（Workflow State）：LangGraph 管理澄清、生成、门禁校验与有限修复，通过 PostgreSQL Checkpoint 跨请求持久化与恢复
  - 底层执行状态（Execution State）：记录实际任务运行实例（ExecutionRun）、SeaTunnel 作业 ID 与指标
- `prepare → approve → commit` 三阶段执行协议：
  1. `Prepare` 阶段：推导输入指纹、资源范围、影响范围、数据分级、运行预算与回滚方案，冻结生成 Preparation 单，不产生外部副作用
  2. `Approve` 阶段：具名审批人基于冻结事实执行独立审批，强制四眼原则与职责分离
  3. `Commit` 阶段：服务端重新比对指纹与审批事实，签发 Ed25519 签名短时单次 Capability 令牌，经 Tool Broker 在同一 PostgreSQL 事务中原子创建 ExecutionRun 与 Outbox 投递命令
- 防重放与单次能力规则：Capability 令牌绑定工具、主体、环境与制品指纹，有效期 5 分钟且由 Replay Guard 保证只能消费一次
- 质量分流与原子发布规则：
  - 有效数据写入影子表，错误数据按质量契约分流至专用错误表并打标错误码
  - 校验通过后通过原子 Swap（如 Doris `REPLACE TABLE ... swap=true`）将影子表替换为正式表

#### 2.8.5 前端模块要求
- `连接与 Profile 管理模块`：处理数据连接表单、连通性测试、元数据安全探查与脱敏样本渲染
- `Pipeline Studio 交互模块`：处理自然语言提示词输入、LangGraph 澄清交互、HOCON 语法高亮与 DAG 图形化渲染
- `不可变版本与 Diff 模块`：对比不同 Pipeline 版本的 Schema、HOCON 配置与质量规则差异
- `审批流与准备单模块`：展示 Preparation 冻结事实、影响分析、回滚方案与具名审批操作卡片
- `运行监控与指标模块`：轮询或订阅执行状态、展示吞吐曲线、质量分流统计与可解释诊断面板
- `安全治理与评测模块`：展示 Benchmark 测评用例集、触发评测并渲染安全指标大盘

#### 2.8.6 后端与 Harness 核心模块要求
- `API 控制面服务`：提供连接、元数据、设计生成、审批、执行与运行监控 RESTful 接口
- `LangGraph 编排引擎`：构建可恢复的 ETL 生成状态机（意图解析 → 元数据探查 → 生成 EtlPlan/HOCON → 确定性门禁 → 试运行 → 准备审批）
- `Harness 安全执行内核`：
  - `Policy Decision Point (PDP)`：根据 ToolIntent、资源范围、环境与数据分级输出 P0-P3 风险决策与审批要求
  - `Capability Issuer & Verifier`：基于 Ed25519 算法签发与校验单次执行能力令牌
  - `Tool Broker & Replay Guard`：作为所有副作用执行的唯一出口，拦截未授权调用与重放请求
  - `Transactional Outbox`：保证本地业务事实与 Worker 任务投递的原子落库
  - `Evidence Ledger`：维护关键事件的哈希链与签名检查点，保证审计证据不可篡改
- `Celery Worker 执行服务`：接收验签后的受管命令，物化 Secret，调用 SeaTunnel 执行作业并回传状态与指标
- `运行时监督引擎`：依据 `RuntimeSupervisionContract` 实时监控记录数、字节数、时长、输出放大比与拒绝率，触发预警或硬中断

#### 2.8.7 数据表要求
- `users`：`id`、`username`、`display_name`、`email`、`status`、`created_at`、`updated_at`
- `projects`：`id`、`name`、`code`、`description`、`created_at`、`updated_at`
- `project_memberships`：`id`、`project_id`、`user_id`、`role`、`created_at`、`updated_at`
- `project_role_grants`：`id`、`project_id`、`user_id`、`role_slot`、`created_at`、`updated_at`
- `pipelines`：`id`、`project_id`、`name`、`code`、`description`、`status`、`created_at`、`updated_at`
- `pipeline_versions`：`id`、`pipeline_id`、`version_number`、`etl_plan_json`、`hocon_text`、`artifact_digest`、`is_immutable`、`created_at`、`updated_at`
- `pipeline_artifacts`：`id`、`version_id`、`artifact_type`、`artifact_digest`、`content`、`created_at`
- `agent_runs`：`id`、`version_id`、`thread_id`、`prompt`、`status`、`step_count`、`error_message`、`created_at`、`updated_at`
- `approval_requests`：`id`、`preparation_id`、`version_id`、`required_role`、`status`、`decision`、`approver_id`、`decided_at`、`created_at`、`updated_at`
- `execution_runs`：`id`、`version_id`、`preparation_id`、`capability_token_digest`、`status`、`engine_job_id`、`input_records`、`output_records`、`error_records`、`bytes_processed`、`started_at`、`finished_at`、`created_at`、`updated_at`
- `file_assets`：`id`、`project_id`、`file_name`、`file_path`、`file_size`、`file_format`、`schema_json`、`created_at`、`updated_at`
- `runtime_supervision_snapshots`：`id`、`execution_run_id`、`metrics_json`、`decision`、`action_taken`、`created_at`
- `audit_events`：`id`、`project_id`、`actor_id`、`event_type`、`resource_type`、`resource_id`、`payload_digest`、`prev_event_hash`、`event_hash`、`created_at`

#### 2.8.8 接口要求
- `GET /health`：检查系统组件与依赖就绪状态
- `GET /api/v1/projects/{project_id}/connections`：查询项目数据源连接列表
- `POST /api/v1/connections` / `PUT /api/v1/connections/{id}`：创建与编辑数据源连接
- `POST /api/v1/connections/{id}/tests`：测试数据源连通性
- `POST /api/v1/connections/{id}/profiles`：发起只读元数据探查与脱敏样本采集
- `POST /api/v1/file-assets`：上传并解析异构文件数据源
- `POST /api/v1/pipelines`：创建 Pipeline 项目定义
- `POST /api/v1/versions/{version_id}/generation`：输入自然语言需求，触发 LangGraph 候选生成
- `POST /api/v1/agent-runs/{run_id}/answers`：提交澄清问题回答，恢复 Agent 状态机
- `GET /api/v1/versions/{version_id}/design`：查询生成的 EtlPlan、HOCON 配置、DAG 拓扑与质量契约
- `POST /api/v1/versions/{version_id}/prepare`：生成 Preparation 准备单，推导影响范围与回滚方案
- `POST /api/v1/approval-requests/{approval_id}/decisions`：提交具名审批决策（approve / reject）
- `POST /api/v1/preparations/{preparation_id}/commit`：校验审批与指纹，签发 Capability 并原子提交执行
- `GET /api/v1/execution-runs/{id}`：查询任务执行状态、指标与质量报告
- `POST /api/v1/execution-runs/{id}/cancel`：请求取消运行中作业
- `POST /api/v1/execution-runs/{id}/rollback`：触发受管影子表回滚与清理
- `POST /api/v1/benchmarks/run`：触发自动化安全与准确率 Benchmark 评测

#### 2.8.9 质量契约与运行监督规则
- 质量契约（QualityContract）：
  - 声明字段级清洗过滤规则（如非空校验、正数校验、邮箱脱敏格式）
  - 配置错误数据流向与错误码标签（ErrorCode）
  - 数据面执行时将未命中规则的数据分流写入错误表，记录原始值与违规原因
- 运行时监督契约（RuntimeSupervisionContract）：
  - 冻结最大读取行数、最大写入字节数、最大执行时长预算
  - 监控输出放大比与错误拒绝率阈值
  - 越线时按策略触发预警、硬中断（Kill Job）或隔离告警

#### 2.8.10 核心业务流程
- `端到端需求生成与受管执行流程`：
  1. 数据工程师配置源库（MySQL）与目标库（Doris）连接，生成受管元数据 Profile
  2. 数据工程师在 Studio 输入自然语言 ETL 需求
  3. LangGraph 状态机解析需求，必要信息缺失时触发中断提问，工程师补充回答后从 Checkpoint 恢复
  4. 模型生成结构化 EtlPlan 与 SeaTunnel HOCON 配置，通过语法编译器与确定性门禁校验
  5. 冻结生成不可变 PipelineVersion 并计算 SHA256 摘要
  6. 调用 Prepare 接口生成准备单，PDP 计算风险等级并分配数据审批人与安全审批人职责槽
  7. 数据审批人与安全审批人分别完成独立审批（四眼原则）
  8. 操作员调用 Commit 接口，服务端校验审批与指纹后签发单次 Capability，写入 Outbox
  9. Celery Worker 消费任务，验签后调用 SeaTunnel 执行搬运
  10. SeaTunnel 将有效数据写入影子表，错误数据写入错误表，监督引擎持续监控预算
  11. 质量报告通过后，Doris 执行原子 Swap 发布正式表，关键链路生成防篡改证据账本
- `任务失败诊断与安全回滚流程`：
  1. 执行异常或监督超限时触发中断，记录失败快照
  2. 诊断服务提取错误堆栈，生成可解释根因说明与修复建议
  3. 操作员发起回滚指令，Harness 验证权限后调用受管清理逻辑恢复原状态

#### 2.8.11 验收标准
- 能在项目内配置独立的数据工程师、数据审批人与安全审批人，系统强制拦截申请人自批与职责混用
- 能接入异构数据库与文件数据源，完成安全元数据探查并展示脱敏样本，系统全链路无敏感明文泄露
- 能通过自然语言需求触发 LangGraph 状态机生成 EtlPlan 与 SeaTunnel HOCON 配置，并在缺少参数时正确中断与恢复
- 能通过确定性门禁对生成的 Pipeline 配置进行语法与 Schema 校验，通过后生成不可变版本摘要
- 能完整执行 `prepare → approve → commit` 三阶段协议，成功生成准备单、完成四眼审批并签发单次 Capability
- 能通过 Celery Worker 调度 SeaTunnel 完成数据搬运，实现有效数据影子表写入与错误数据质量分流
- 能在数据达标后通过原子 Swap 完成正式表发布，并在运行中心查看实时指标与质量报告
- 能在超预算或异常情况下触发运行时监督与可解释根因诊断，并支持受管回滚
- 能运行自动化 Benchmark 评测集，输出准确率与安全拦截率评测报告

### 2.9 知识库管理平台（*）
- 项目目标：实现一个集知识维护、多维权限管理、AI 问答鉴权检索、数据看板与知识自动沉淀于一体的知识库管理平台，支持单篇与批量知识导入、细粒度操作与数据权限控制、智能体鉴权召回、热点 FAQ 挖掘沉淀与知识缺口识别
- 交付形式：提供可运行前端、后端服务、数据库初始化脚本、示例知识文档数据、接口说明文档与完整业务演示链路

#### 2.9.1 开发范围
- 知识维护能力：单文件导入、批量文件导入、知识单元拆分与解析、知识单元增删改查、知识版本与状态管理
- 组织与权限能力：用户登录认证、用户管理、角色管理、部门管理、菜单与操作权限配置、四维混合数据权限（全局/部门/角色/个人）配置
- AI 检索与问答鉴权能力：用户登录态校验、智能体知识召回、数据权限鉴权过滤、未授权拦截与友好提示、问答会话管理
- 数据看板能力：Agent 访问量与人数统计、知识单元总量统计、常见问题排行榜、高频访问知识单元排行榜、Token 消耗统计、响应时间统计
- 知识沉淀能力：高频问题挖掘与自动推荐为候选 FAQ、管理员审核发布、FAQ 问答缓存加速、知识缺口识别与未命中问题分析

#### 2.9.2 用户角色与权限
- `系统管理员`：创建和维护用户、角色、部门；分配菜单与操作权限（增、删、改、查、AI访问）；监控系统看板与 Token 消耗
- `知识管理员`：导入、批量导入、编辑、删除知识单元；配置知识单元的数据权限（全局/部门/角色/个人）；审核并发布推荐 FAQ；维护知识缺口
- `普通用户 / 提问者`：登录系统进行 AI 智能问答，根据被分配的数据权限获取对应知识单元的回答内容，对无权限知识单元接收权限缺失提示

#### 2.9.3 页面需求
- `登录与个人中心页`：提供用户名密码登录，展示当前用户身份、所属部门与拥有角色
- `组织架构与权限管理页`：
  - `用户管理`：展示用户列表、部门归属、角色关联，支持新增、编辑、重置密码、启停用
  - `角色管理`：维护角色名称、角色编码与操作权限树（增删改查、AI访问权限）
  - `部门管理`：维护部门树形结构、负责人与成员关联
- `知识维护与导入页`：
  - `知识导入中心`：提供单文件上传与多文件/目录批量导入界面，支持 PDF、Markdown、Word、TXT 格式，展示解析进度与结果
  - `知识单元列表`：展示知识单元编号、标题、分类、格式、数据权限摘要、创建人、更新时间与状态，支持搜索、筛选、查看、编辑与删除
  - `知识单元编辑/详情页`：编辑知识标题、正文内容、标签、附件，以及设置数据权限实体（全局/部门/角色/个人混合分配）
- `AI 对话鉴权工作台`：要求用户登录后方可进入，提供智能提问输入框、历史对话列表、流式回复展示、知识引用来源展示，以及无权限知识单元被召回时的明确缺失提示
- `数据看板页`：
  - 指标卡片：Agent 访问次数、独立访问人数（UV）、知识单元总数、总 Token 消耗、平均响应时间
  - 排行榜：常见高频问题 TOP 榜、最常访问知识单元 TOP 榜
  - 趋势与分布图：按日/周访问趋势、Token 消耗走势、响应时间分布
- `知识沉淀管理页`：
  - `FAQ 自动推荐与审核`：展示系统从历史对话挖掘的高频问题推荐列表、推荐频次、关联知识单元、建议答案，提供审核通过（发布上线并写入缓存）与驳回操作
  - `已发布 FAQ 库`：管理已上线的 FAQ 问答与缓存生效状态
  - `知识缺口列表`：展示 AI 检索未命中或置信度过低的用户提问、提问频次、最近提问时间，支持一键创建关联知识单元补全需求

#### 2.9.4 知识单元与权限规则
- 知识单元规则：每个独立导入的文档或手册作为一个知识单元（Knowledge Unit）存储并拥有唯一标识
- 操作权限规则：按角色配置功能菜单访问、知识单元增删改查、AI 问答访问权限
- 数据权限规则：
  - 默认状态下知识单元无任何访问权限
  - 知识单元支持配置四类权限实体：`全局（global）`、`部门（department）`、`角色（role）`、`个人（user）`，支持多实体混合分配
  - 满足配置的数据权限实体中的任意一种（OR 逻辑）即可访问该知识单元
- AI 问答鉴权规则：
  - 智能体回答问题前必须校验用户登录态，获取用户所属部门与角色列表
  - 智能体检索召回候选知识单元后，调用知识库管理平台权限鉴权接口
  - 仅对满足数据权限的知识单元进行内容拼装并生成回答
  - 召回的知识单元中存在用户无权访问的内容时，在回复或引用来源中明确告知缺失对应知识单元的访问权限
- 缓存加速规则：审核发布的 FAQ 写入高速缓存，匹配到用户相同或高度相似问题时直接命中缓存输出

#### 2.9.5 前端模块要求
- `认证与权限控制模块`：管理 Token、用户登录态、动态菜单渲染与按钮级操作权限控制
- `组织架构管理模块`：处理用户、角色与部门的树形/列表展示、增删改查与角色权限分配
- `知识管理与批量导入模块`：支持文件拖拽上传、批量文件并发上传、解析状态进度轮询、知识单元 CRUD
- `数据权限配置组件`：提供统一的权限选择弹窗，支持勾选全局公开、多选部门、多选角色、多选人员
- `AI 对话工作台模块`：提供多轮对话、流式 Markdown 渲染、知识引用卡片与权限缺失提示卡片
- `看板与图表模块`：渲染核心指标统计卡、高频问题排行榜、知识单元热度榜与 Token/耗时趋势图表
- `知识沉淀模块`：处理 FAQ 自动推荐列表、审核流转操作、知识缺口列表与快速建档转化

#### 2.9.6 后端与服务模块要求
- `认证鉴权模块`：负责用户认证、JWT 令牌签发与校验、RBAC 操作权限拦截
- `组织架构服务`：维护用户、角色、部门树及人员与角色/部门的映射关系
- `知识单元管理服务`：负责文档上传、格式解析、文本切片、知识单元 CRUD 与向量化同步
- `数据权限引擎`：负责计算并校验用户对指定知识单元集合的访问权限，输出允许列表与拒绝列表
- `AI 鉴权检索服务`：负责会话管理、向量检索/关键字混合召回、权限过滤、Prompt 组装、流式回答生成与权限缺失提示
- `数据看板统计服务`：异步记录每轮问答的访问日志（用户、耗时、Token 消耗、命中知识单元、提问内容），聚合计算访问量、排行榜与性能指标
- `知识沉淀挖掘服务`：定时或基于聚类算法挖掘历史对话中的高频相似问题，生成待审核 FAQ 推荐项；识别未命中知识库的提问记录生成知识缺口项
- `FAQ 缓存服务`：管理审核通过的 FAQ 问答对，提供精确或语义相似度匹配与快速应答缓存

#### 2.9.7 数据表要求
- `users`：`id`、`username`、`password_hash`、`display_name`、`department_id`、`status`、`created_at`、`updated_at`
- `departments`：`id`、`parent_id`、`name`、`leader_id`、`sort_order`、`created_at`、`updated_at`
- `roles`：`id`、`role_name`、`role_code`、`description`、`created_at`、`updated_at`
- `user_roles`：`id`、`user_id`、`role_id`、`created_at`
- `role_permissions`：`id`、`role_id`、`permission_code`、`permission_type`、`created_at`
- `knowledge_units`：`id`、`unit_code`、`title`、`content`、`summary`、`category`、`source_file_name`、`file_type`、`file_size`、`status`、`creator_id`、`created_at`、`updated_at`
- `unit_permissions`：`id`、`unit_id`、`target_type`（`global`、`department`、`role`、`user`）、`target_id`、`created_at`
- `qa_access_logs`：`id`、`session_id`、`user_id`、`question`、`answer`、`recalled_unit_ids_json`、`authorized_unit_ids_json`、`unauthorized_unit_ids_json`、`prompt_tokens`、`completion_tokens`、`total_tokens`、`response_time_ms`、`created_at`
- `faqs`：`id`、`question`、`answer`、`category`、`related_unit_id`、`source_type`（`manual`、`auto_mined`）、`status`（`pending_review`、`published`、`rejected`）、`hit_count`、`reviewer_id`、`reviewed_at`、`created_at`、`updated_at`
- `knowledge_gaps`：`id`、`question_pattern`、`sample_questions_json`、`ask_count`、`last_asked_at`、`status`（`unresolved`、`resolved`、`ignored`）、`resolved_unit_id`、`created_at`、`updated_at`

#### 2.9.8 接口要求
- `POST /api/auth/login`：请求字段 `username`、`password`，响应 `access_token`、`user_info`、`permissions`
- `GET /api/org/departments`：获取部门树形列表
- `POST /api/org/users` / `PUT /api/org/users/{id}`：用户新增与编辑
- `GET /api/org/roles` / `POST /api/org/roles/{id}/permissions`：角色列表与权限分配
- `POST /api/knowledge/import`：支持单文件或多文件批量上传与解析入库
- `GET /api/knowledge/units`：按标题、分类、状态分页查询知识单元列表
- `GET /api/knowledge/units/{id}`：查询知识单元详情与已配置的数据权限列表
- `PUT /api/knowledge/units/{id}`：更新知识单元内容
- `POST /api/knowledge/units/{id}/permissions`：批量配置知识单元数据权限实体
- `DELETE /api/knowledge/units`：批量删除知识单元
- `POST /api/knowledge/check-permissions`：请求字段 `user_id`、`unit_ids`，响应 `authorized_unit_ids`、`unauthorized_unit_ids`
- `POST /api/ai/chat/stream`：请求字段 `question`、`session_id`，校验登录态与数据权限，返回 SSE 流式应答与权限缺失提示
- `GET /api/dashboard/metrics`：查询访问总次数、独立用户数、知识单元数、Token 总量与平均耗时
- `GET /api/dashboard/rankings/questions`：查询常见问题 TOP 榜
- `GET /api/dashboard/rankings/units`：查询最常访问知识单元 TOP 榜
- `GET /api/dashboard/stats/tokens`：查询 Token 消耗与响应时间趋势
- `GET /api/settlement/faqs/recommendations`：查询自动推荐的待审核 FAQ 列表
- `POST /api/settlement/faqs/{id}/review`：请求字段 `action`（`approve` / `reject`）、`edited_answer`
- `GET /api/settlement/knowledge-gaps`：查询知识缺口列表与未命中频次

#### 2.9.9 看板与知识沉淀规则
- 数据看板聚合规则：
  - 每次问答记录独立日志，提取提问词、命中知识单元、Token 计数和接口响应时长
  - 每日与实时汇总访问指标、热度榜单与性能分布
- FAQ 挖掘与审核发布规则：
  - 对历史提问进行语义去重与频次聚合，当某类问题出现频次达到阈值时自动生成推荐 FAQ 项
  - 管理员审核确认后标记为 `published` 并写入缓存
  - 智能体对话优先匹配已发布的 FAQ 缓存，提高响应速度并降低模型 Token 消耗
- 知识缺口识别规则：
  - 提问召回相似度低于设定阈值、或无可用知识单元支撑的问答记录，标记为知识缺口
  - 统计缺口提问频次并聚合聚类，为知识管理员补充知识提供依据

#### 2.9.10 核心业务流程
- `知识批量维护与权限配置流程`：
  1. 知识管理员在导入界面上传单篇或多篇文档
  2. 系统解析文档内容、拆分为独立知识单元并完成向量索引入库
  3. 管理员为知识单元配置数据权限（选择全局公开、或勾选指定部门、角色、个人）
  4. 保存权限规则并即时生效
- `AI 鉴权问答流转流程`：
  1. 用户登录系统进入 AI 对话窗口发起提问
  2. 智能体检索知识库召回候选知识单元
  3. 智能体调用数据权限审查接口，传入用户身份与召回知识单元 ID
  4. 鉴权引擎校验全局、部门、角色、个人四种实体匹配情况（满足任意一种即放行）
  5. 权限通过的知识单元组装至 Context 生成回答
  6. 对召回但无权限的知识单元，在回复中明确提示用户缺失对应知识单元的访问权限
  7. 异步记录访问日志（包含 Token 消耗、响应时间、命中单元与用户 ID）
- `知识沉淀与 FAQ 闭环流程`：
  1. 沉淀引擎分析问答日志，将高频问题自动推荐为候选 FAQ
  2. 知识管理员在沉淀页面查看推荐列表，审核通过并可编辑标准化答案
  3. 审核通过后发布上线并注入问答缓存
  4. 后续用户提问命中 FAQ 缓存时直接输出标准答案
  5. 沉淀引擎汇总未命中知识库的问题生成知识缺口清单，供管理员定向补充新知识

#### 2.9.11 验收标准
- 能完成用户登录、角色管理、部门管理与菜单/按钮级操作权限分配
- 能通过界面完成单篇与批量文档导入，并成功将文档拆分为知识单元完成检索入库
- 能对知识单元执行增删改查，并支持混合配置全局、部门、角色、个人四类数据权限
- 能在用户满足部门、角色、个人或全局中任意一种权限时正常访问知识单元
- 能在 AI 对话中强制校验登录态并调用鉴权接口过滤未授权知识单元，对无权限召回项输出明确权限缺失提示
- 能在数据看板正确展示访问次数、独立人数、知识单元数、常见问题榜、知识热度榜、Token 消耗与响应时间趋势
- 能自动从历史对话中挖掘高频问题并生成推荐 FAQ，支持管理员审核发布并写入缓存加速应答
- 能自动识别未命中知识库的问题并生成知识缺口列表，支持查看提问频次

## 3. 其他工具开发
### 通用开发要求
- 基础交付：每个工具至少支持一种实际数据输入方式，输出结构化结果、判断依据和处理建议
- 结果验证：提供可直接运行的演示数据和完整操作流程，支持查看输入、分析结果和异常提示
- 按需建设：涉及付款、审批、录用、处罚或敏感数据的工具增加人工复核、权限控制和操作记录，其他工具不强制实现完整管理流程

### 3.1 行政管理
#### 3.1.1 会议纪要与行动项跟踪器
- 要解决的问题：会议结束后行动项容易漏跟，责任人、截止时间和后续状态无法持续追踪
- 工具功能：从单次会议的录音转写或会议纪要中提取结论、待办、责任人和截止时间，生成会议行动项清单并跟踪完成状态

#### 3.1.2 资产异常巡检助手
- 要解决的问题：办公资产领用、归还、闲置状态靠人工表格维护，容易出现账实不符
- 工具功能：结合资产台账、扫码盘点记录、资产照片和维修记录，识别超期未还、长期闲置、重复领用、标签异常和盘点差异

### 3.2 人力资源
#### 3.2.1 候选人匹配筛查助手
- 要解决的问题：简历筛选依赖人工阅读，容易漏掉关键经历或忽略硬性条件
- 工具功能：根据岗位要求提取技能、年限、项目经历和风险点，输出匹配评分和待复核项

#### 3.2.2 面试一致性评估助手
- 要解决的问题：多轮面试评价标准不统一，面试官评分口径和证据充分性难以横向比较
- 工具功能：对接岗位能力模型、面试题库和多轮面试记录，检测评分偏差、证据不足、追问遗漏和评价冲突

### 3.3 财务管理
#### 3.3.1 发票报销审核助手
- 要解决的问题：报销审核需要反复核对发票、审批单和预算规则，人工审核耗时且容易漏检
- 工具功能：识别发票、行程单和审批单，对照报销制度、预算与历史入账记录校验材料完整性和财务合规性，推荐费用科目并输出通过、退回或人工复核结论

#### 3.3.2 预算执行偏差监控助手
- 要解决的问题：部门、项目和费用科目的实际支出偏离预算后难以及时发现
- 工具功能：按部门、项目、费用科目和期间对比预算与实际支出，识别超预算、执行进度异常和异常增长，输出偏差金额、偏差比例和责任范围

#### 3.3.3 应收账款逾期预警助手
- 要解决的问题：应收账款逾期会影响现金流，人工跟踪容易遗漏高风险客户
- 工具功能：结合应收账龄、合同账期、客户历史付款和催收记录，预测逾期风险并输出高风险客户、应收金额、预计逾期时间和催收优先级

### 3.4 法务管理
#### 3.4.1 合同签署前风险审查助手
- 要解决的问题：合同风险点分散在付款、违约、保密、知识产权等条款中，人工审查容易漏掉关键风险
- 工具功能：识别高风险条款，并按风险等级、影响范围和建议处理方式输出审查结果

#### 3.4.2 合同修订差异审查助手
- 要解决的问题：合同多轮修改后很难看清关键变化
- 工具功能：标记新增、删除、修改条款，并提示对责任、金额、期限和违约后果的影响

#### 3.4.3 合同签署后履约跟踪器
- 要解决的问题：合同签完后付款、交付、验收、续约等关键义务容易没人跟
- 工具功能：抽取合同中的付款、交付、验收、续约、终止等节点并生成提醒

### 3.5 采购管理
#### 3.5.1 供应商准入与履约评估助手
- 要解决的问题：供应商比价不只看价格，还要兼顾交期、付款、历史交付和售后
- 工具功能：在供应商准入和复评阶段汇总报价、资质、历史交付、质量与售后记录，输出分项评分、准入建议和履约风险

#### 3.5.2 采购申请完整性检查助手
- 要解决的问题：采购需求描述不清会导致反复补充信息，影响询价和审批效率
- 工具功能：在询价前对照历史采购、库存、预算和验收规则检查采购申请，识别缺失字段、规格歧义和异常数量，生成补充确认清单

#### 3.5.3 采购合同订单发票匹配助手
- 要解决的问题：采购合同、订单、发票之间容易出现金额或交期不一致
- 工具功能：从非结构化合同、订单和发票中识别供应商、金额、数量、交期等字段，自动比对并标记差异

### 3.6 销售管理
#### 3.6.1 销售商机停滞风险分析助手
- 要解决的问题：商机停滞、关键人缺失或竞品介入后，销售往往发现较晚
- 工具功能：识别长时间无进展、关键人缺失、预算不明确、竞品介入和报价异常

#### 3.6.2 销售报价与折扣合规检查助手
- 要解决的问题：报价与客户需求、折扣权限、合同条款之间容易不一致
- 工具功能：校验报价单、折扣、产品配置和审批权限，标记不合规项

### 3.7 客户服务
#### 3.7.1 全渠道反馈工单化与智能分派助手
- 要解决的问题：用户反馈分散在客服会话、评论、社群和问卷中，人工建单、去重、分类和分派效率低
- 工具功能：汇集多渠道反馈并完成聚类、去重和工单转换，根据问题描述、产品、订单与历史处理记录判断类别、优先级和负责团队

#### 3.7.2 高风险投诉升级预警助手
- 要解决的问题：高风险投诉没有及时升级，容易演变为舆情或重大客户风险
- 工具功能：识别负面情绪、重复投诉、金额争议、舆情风险和 SLA 超时风险

#### 3.7.3 客服工单共性问题根因分析助手
- 要解决的问题：同类问题重复发生但难以及时反馈给产品或运营
- 工具功能：对已处理工单进行主题聚类，关联订单、产品版本和处理结果，识别重复问题、可能根因与影响范围，并生成产品或运营改进待办

### 3.8 市场营销
#### 3.8.1 广告投放异常诊断助手
- 要解决的问题：投放效果波动大，人工排查点击率、转化率和成本异常耗时
- 工具功能：对接广告平台的账户、计划、素材和人群数据，识别点击率、转化率和成本异常，输出异常层级、影响范围、可能原因和调整建议

#### 3.8.2 市场调研洞察分析助手
- 要解决的问题：问卷和访谈数据量大，目标人群的需求差异、购买动机和市场机会难以提炼
- 工具功能：结合问卷、访谈和用户画像，按人群、场景、需求、购买动机与阻碍因素分类，输出细分人群特征、市场洞察和机会方向

### 3.9 业务运营
#### 3.9.1 用户运营漏斗异常诊断助手
- 要解决的问题：获客、激活、转化、留存和复购环节发生异常后，运营人员难以快速定位受影响的人群和流程节点
- 工具功能：监控访问、注册、激活、转化、留存和复购指标，按人群、渠道、地区、版本与时间拆解异常，输出漏斗损耗节点和运营待办

#### 3.9.2 运营活动全周期评估助手
- 要解决的问题：活动立项缺少数据依据，活动结束后也难以判断效果不佳的具体原因
- 工具功能：面向促销、拉新、召回和会员活动，活动前根据历史数据、预算、目标人群与规则评估可行性，活动后结合成本、漏斗和用户行为输出效果与归因结果

### 3.10 产品管理
#### 3.10.1 产品需求价值评估助手
- 要解决的问题：需求优先级容易被主观判断影响，难以解释取舍依据
- 工具功能：对已进入需求池的产品需求，结合用户覆盖、业务价值、战略匹配度、实现成本和风险形成价值评分与优先级建议

#### 3.10.2 产品交付物一致性检查助手
- 要解决的问题：需求、原型、接口、埋点和测试用例之间容易不一致，导致上线后返工
- 工具功能：对接 PRD、原型说明、接口文档、埋点方案和测试用例，检查字段、流程、权限、异常状态和验收条件是否一致

#### 3.10.3 上线版本问题归因助手
- 要解决的问题：上线后问题难以快速归因到需求、设计还是实现
- 工具功能：针对指定上线版本关联异常日志、监控指标、工单、代码变更、测试记录和需求文档，判断问题来源属于需求遗漏、设计缺陷、实现错误、配置变更或数据异常

### 3.11 教学实施
#### 3.11.1 教学学情诊断与分层练习助手
- 要解决的问题：作业、随堂测和课堂互动数据分散，教师难以及时识别个人与班级的知识点缺口并安排差异化练习
- 工具功能：结合评分标准、作业、随堂测、课堂互动和课程进度，识别学生错因、个人薄弱点与班级共性卡点，生成补讲清单和分层练习建议

### 3.12 培训运营
#### 3.12.1 培训结业与发证风险预警助手
- 要解决的问题：报名、出勤、测评和证书数据分散，学员是否满足结业或发证条件难以及时判断
- 工具功能：结合报名信息、出勤记录、考试成绩、作业和发证规则，识别结业风险、证书发放异常和需要运营跟进的学员

#### 3.12.2 培训成效与课程改进分析助手
- 要解决的问题：培训效果只看满意度，测评、业务表现和学员反馈难以形成统一的改进依据
- 工具功能：对比课前测评、课后测评、作业和业务指标变化，结合反馈内容、课程版本与讲师安排分析问题来源，输出能力提升结果、未达标人群和课程改进待办

### 3.13 团队管理
#### 3.13.1 团队人员产能与负载分析助手
- 要解决的问题：人员负载不均衡，容易出现关键人过载或资源闲置
- 工具功能：汇总成员在多个项目和日常工作中的任务数量、优先级、计划工时与截止时间，输出个人负载、团队可用产能和任务调配建议

#### 3.13.2 绩效证据归集与事实核验助手
- 要解决的问题：绩效评价缺少持续积累的可验证证据，评价表述与实际贡献也难以核对
- 工具功能：关联目标、任务、交付物、协作记录和业务指标，按人员与考核周期生成证据档案，校验评价中的事实、时间、贡献和结果并标记证据缺口

### 3.14 项目管理
#### 3.14.1 单项目进度风险预测助手
- 要解决的问题：项目延期风险通常到临近交付才暴露
- 工具功能：针对单个项目分析里程碑、关键路径、任务依赖、完成率和变更频率，预测延期风险并输出受影响里程碑、交付物和赶工优先级

#### 3.14.2 项目需求变更影响评估助手
- 要解决的问题：需求变更导致范围蔓延，影响排期和成本
- 工具功能：对比变更前后的需求范围，识别受影响的任务、里程碑、接口、测试、人员、成本和交付物，输出变更影响清单与重新排期建议

#### 3.14.3 跨部门协作阻塞诊断助手
- 要解决的问题：跨部门协作责任边界不清，阻塞事项容易长期无人处理
- 工具功能：从跨部门任务流转、审批和沟通记录中识别未确认责任人、等待外部输入、等待审批和长期无响应事项，输出阻塞链路与升级对象

### 3.15 经营分析
#### 3.15.1 经营财务指标异常诊断助手
- 要解决的问题：收入、毛利、费用、库存周转和经营现金流等公司级指标异常跨越多个系统，经营分析人员难以及时定位来源
- 工具功能：汇总销售、成本、费用、库存和现金流数据，监控公司级经营财务指标，按业务线、区域、产品和期间拆解异常，输出经营影响与管理层关注事项

#### 3.15.2 经营目标情景测算助手
- 要解决的问题：年度目标和经营调整缺少收入、成本、现金流与资源约束的联动测算
- 工具功能：输入收入增长、价格、成本、人员和预算等变量，生成不同情景下的利润、现金流、资源缺口、风险阈值和目标达成情况对比

#### 3.15.3 竞品异动经营影响评估助手
- 要解决的问题：竞品价格、渠道和产品变化影响业务但难以及时量化
- 工具功能：跟踪竞品变化并关联自身销售、客诉和市场数据判断影响范围

## 4. 模型微调
### 4.1 知识库项目模型微调
- 模型用途：根据检索系统召回的文档片段、表格内容、制度条款或产品资料生成答案
- 微调目标：提升模型基于检索内容作答的忠实性、完整性、可追溯性和拒答能力

#### 4.1.1 行业术语与回答格式适配能力
- 需要提升的能力：按照知识库所属行业的术语、口径和输出格式回答，例如制度解释、产品说明、操作步骤、故障处理
- 训练数据：行业问答样例、标准术语表、不同格式的答案模板
- 评估重点：术语是否准确，格式是否稳定，回答是否符合业务口径

#### 4.1.2 检索内容忠实回答能力
- 需要提升的能力：严格依据检索结果回答问题，避免编造未出现在资料中的事实、数字、政策和结论
- 训练数据：用户问题、检索片段、标准答案、不可回答样例
- 评估重点：答案是否有依据、是否引入幻觉、是否遗漏关键条件

#### 4.1.3 多片段信息整合能力
- 需要提升的能力：从多个检索片段中合并同一主题的信息，处理上下文分散、表述不一致和跨段落引用
- 训练数据：多段检索结果、跨文档问题、整合后的标准答案
- 评估重点：信息整合是否完整，是否能区分主次、条件和适用范围

#### 4.1.4 证据引用与出处标注能力
- 需要提升的能力：在答案中标注依据来源，说明结论来自哪些检索片段或文档条款
- 训练数据：带来源编号的检索结果、带引用标注的答案样例
- 评估重点：引用是否准确，答案内容和引用证据是否一致

#### 4.1.5 资料不足时的拒答能力
- 需要提升的能力：当检索结果不足、无关或互相矛盾时，不强行回答，并提示缺少哪些信息
- 训练数据：无召回、弱相关召回、冲突召回和标准拒答样例
- 评估重点：拒答是否及时，是否能指出缺失信息和下一步补充方向

### 4.2 问数项目模型微调
- 模型用途：根据检索出的表结构、字段信息、字段值、指标定义和计算规则生成 SQL
- 微调目标：提升模型从自然语言问题到可执行 SQL 的字段匹配、指标计算、条件构造和澄清能力

#### 4.2.1 业务问题到字段映射能力
- 需要提升的能力：将用户自然语言中的业务概念映射到正确的数据表、字段、枚举值和时间字段
- 训练数据：用户问题、检索出的 schema、字段说明、字段值样例、标准 SQL
- 评估重点：表字段选择是否正确，过滤条件是否符合业务含义

#### 4.2.2 字段值识别与标准化能力
- 需要提升的能力：将用户输入中的别名、简称、口语表达映射为数据库中的标准字段值
- 训练数据：字段值字典、同义词、历史查询问题、标准 SQL
- 评估重点：枚举值是否匹配，模糊值是否需要澄清，是否误用不存在的字段值

#### 4.2.3 指标口径理解与计算能力
- 需要提升的能力：根据检索出的指标定义生成正确的聚合、计算公式、分母分子、去重逻辑和时间口径
- 训练数据：指标说明、计算规则、用户问题、标准 SQL
- 评估重点：指标口径是否正确，聚合函数、去重和时间范围是否准确

#### 4.2.4 查询意图澄清能力
- 需要提升的能力：当用户问题缺少时间范围、统计粒度、指标口径或筛选条件时，主动提出澄清问题
- 训练数据：信息不完整的问题、澄清问题样例、补充信息后的 SQL
- 评估重点：是否能识别缺失条件，澄清问题是否具体可回答

#### 4.2.5 多表关联与查询条件生成能力
- 需要提升的能力：根据主外键、业务关联关系和筛选条件生成 join、where、group by、having、order by 等 SQL 结构
- 训练数据：多表 schema、关联关系、复杂查询问题、标准 SQL
- 评估重点：join 路径是否正确，条件是否遗漏，分组粒度是否合理

#### 4.2.6 SQL 安全与方言适配能力
- 需要提升的能力：生成只读查询语句，适配目标数据库 SQL 方言，避免删除、更新、越权查询和危险函数
- 训练数据：不同数据库方言样例、安全 SQL 样例、危险 SQL 反例
- 评估重点：SQL 是否可执行，是否符合方言，是否满足只读和权限约束

### 4.3 客服项目模型微调
- 模型用途：识别用户意图，进入对应业务流程，抽取槽位信息，并在必要时澄清问题或转人工处理
- 微调目标：提升模型在多轮客服场景中的意图识别、槽位抽取、流程控制、澄清和异常处理能力

#### 4.3.1 用户意图识别能力
- 需要提升的能力：识别用户消息对应的业务意图，例如查询订单、修改信息、申请退款、投诉升级、人工转接
- 训练数据：用户原始消息、意图标签、多意图样例、相似意图区分样例
- 评估重点：意图分类是否准确，是否能识别多意图和模糊意图

#### 4.3.2 槽位信息抽取能力
- 需要提升的能力：从用户消息中抽取订单号、手机号、商品名、时间、地址、金额、问题类型等关键槽位
- 训练数据：用户消息、槽位标注、缺失槽位样例、错误槽位反例
- 评估重点：槽位是否完整、准确，是否能识别缺失和冲突信息

#### 4.3.3 澄清问题生成能力
- 需要提升的能力：当用户诉求不清、槽位缺失或多个流程都可能匹配时，提出具体澄清问题
- 训练数据：模糊用户消息、缺失槽位状态、标准澄清话术
- 评估重点：澄清问题是否必要、具体、一次只问关键缺失项

#### 4.3.4 多轮流程状态管理能力
- 需要提升的能力：根据当前流程、已收集槽位和用户补充信息决定下一步动作
- 训练数据：多轮对话、流程节点、槽位状态、下一步动作标注
- 评估重点：流程跳转是否正确，是否能保持上下文，不重复询问已提供信息

#### 4.3.5 异常处理与人工升级能力
- 需要提升的能力：识别用户情绪升级、规则不可处理、信息冲突、流程无法继续等情况，并进入兜底或人工流程
- 训练数据：异常对话、信息冲突样例、投诉升级样例、人工转接样例
- 评估重点：是否能及时升级，是否避免错误承诺，是否保留必要上下文给人工客服



**拆解功能，细化颗粒度，最好到函数级别。**

**评估，部署，上线**
